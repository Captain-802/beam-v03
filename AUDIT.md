# Beam Designer v03 — structural calculation audit

Audit date: 16 September 2026. Source reviewed: `Captain-802/beam-v03`, starting commit `6c9fcfcadb9464db6c8bf2e0ab2df4a305be487d`.

The Google Sites page embeds `https://captain-802.github.io/beam-v03/`. This audit therefore addresses that repository, including the modular application and `dist/beam-design-single.html`.

## Result

Confirmed defects were found in the original verdict logic, load handling, classification, member interaction and serviceability checks. The branch corrects implemented calculations and prevents PASS where a required combined-action check is missing. It does **not** implement every missing design procedure or certify the calculator for unrestricted structural design.

The adopted basis remains the first-generation EN 1993-1-1 with UK National Annex, and the separate BS 5950-1:2000 option. This is not a conversion to second-generation Eurocodes.

## Findings and corrections

| Finding | Consequence in the original application | Correction |
|---|---|---|
| BS high-shear resistance calculated but bypassed | `Mcx` was reduced for shear, while the verdict used an unreduced `Mrx` | Limit `Mrx` by the applicable shear-reduced `Mcx`; block unimplemented high-shear/axial interaction |
| BS minor-axis moment ignored | Entering a very large `Mz` could leave PASS unchanged | Any nonzero BS minor-axis moment explicitly blocks PASS until the biaxial procedure is implemented |
| No independent BS axial section utilisation | Axial section adequacy depended on indirect bending checks | Add an explicit tensile/compressive section utilisation, retaining member buckling checks |
| Code switch retained EC default ULS factors | Selecting BS retained 1.35G + 1.5Q | Untouched default combination changes to 1.4G + 1.6Q for BS; returning to EC restores its default. Custom combinations are preserved |
| Incomplete moment-diagram combination sweep | The largest peak moment need not govern an equivalent-moment buckling check | BS m-factors and EC member interactions now evaluate every enabled ULS diagram. EC unrestrained interaction uses the minimum computed resistance as a conservative bound |
| Arbitrary diagram treated as linear from one midpoint | Reversing or partial loading could receive an unjustified beneficial EC `Cm` | Test the whole sampled diagram for linearity; use `Cm = 1` for arbitrary/mixed loading outside recognised cases. Read end moments inside the member |
| Finite numbers from an internal-hinge mechanism | Counting restraints did not prove stability; floating-point elimination could return finite displacements | Check the rank of the piecewise-rigid displacement constraints; use scaled Cholesky for the restrained stiffness systems |
| Full member length used for multi-span deflection limit | Two 4 m spans were checked against 8 m/divisor | Check each support interval and end overhang against its own length/divisor; select the governing SLS combination by utilisation |
| EC cold-formed SHS used a BS flat-width ratio | Local buckling slenderness could be understated | EC uses `(h − 3t)/t`; BS retains its separate stored ratio |
| Major-axis classification reused without a biaxial web check | A web/wall in compression from minor-axis bending could retain pure major-axis bending limits | Use a documented conservative uniform-compression web bound for biaxial design; block gross-area compression buckling for a Class 4 compression web |
| EC channel compression missing buckling modes | An advisory allowed PASS without torsional/torsional-flexural compression buckling | Missing clause 6.3.1.4 checks now block PASS |
| EC combined high-shear checks only screened at peak moment | High shear elsewhere could escape the axial or unsupported-family interaction screen | Screen all ULS stations; block missing clause 6.2.10, minor-axis/high-shear, and non-I/Class 1–2 span-wise checks |
| Torsion checked separately from direct axial/minor bending | Separate passing checks do not establish the combined resistance | Direct axial force or imposed minor-axis moment combined with active torsion now blocks PASS pending a unified check |
| SHS torsional table aliases borrowed thicker walls | For example, 40×40×3.0 used the 3.2 mm P385 constants | Remove 3.0/3.5/6.0 mm aliases at lookup; use actual hot-finished geometry when no exact entry is available. Do not use the hot-finished table for CF SHS |
| Hollow-section LTB curve always set to d | Some hollow-section capacities were unnecessarily reduced | Apply the UK NA aspect-ratio allocation, including the separate cold-formed case |
| Warping torsion ignored an entered E | Bending and warping calculations could use different elastic moduli | Use the same entered E in warping stiffness and stresses |
| Invalid or omitted actions accepted silently | Blank input became zero, invalid length reverted to an old value, or active W/E loads were omitted from every ULS combination | Preserve invalid required entries for validation; reject invalid areas/overrides and active load cases absent from all ULS combinations |
| Signed self-weight factor treated inconsistently | A negative G factor applied to user dead loads but omitted automatic self-weight | Apply the signed factor consistently in bending and LTB load construction |
| Report concealed the distinction between failure and incomplete verification | Unsupported cases were simply labelled FAIL; the banner always named the peak-moment combination | Show NOT VERIFIED where checks are missing and no calculated limit is exceeded; identify the governing combination when known |

Some corrections deliberately restrict the calculator's accepted scope. They are not substitutes for the missing structural checks. The uniform-compression classification and minimum-resistance interaction bound are conservative choices, identified as such in the code/report.

## Numerical evidence

The automated suite separates calculation-unit fixtures from complete beam analyses.

- **BS high-shear check fixture:** S275 UB 457×191×82, `M = 420 kNm`, `V = 0.95 Pv`. The original engine calculated `Mcx = 386.594 kNm` but used `Mrx = 503.250 kNm`, permitting PASS in that check fixture. The corrected `Mrx = 386.594 kNm` gives utilisation **1.0864, FAIL**. This fixture isolates resistance/verdict logic; it is not a prescribed physical loading arrangement.
- **Ignored BS minor moment:** a lightly loaded 1 m member with `Mz = 10,000 kNm` previously passed because the action was unused. It is now NOT VERIFIED on the BS path.
- **Two equal spans:** an 8 m continuous member with supports at 0, 4 and 8 m now uses **4000/360 = 11.111 mm**, rather than 8000/360 = 22.222 mm.
- **Hinge mechanism:** fixed support at 0 m, a pin at 1 m and an internal hinge at 2 m on a 3 m member leaves the final segment free to rotate. This now produces an instability error. A stable fixed–hinge–pin Gerber example remains valid, with zero moment at its hinge and correct equilibrium.
- **Elastic buckling benchmark:** `E = 210000 N/mm²`, `G = 81000 N/mm²`, `Iz = 18.7×10⁶ mm⁴`, `It = 692000 mm⁴`, `Iw = 0.922×10¹² mm⁶`, `L = 6000 mm`, fork supports, uniform moment. The eigenvalue result is **Mcr = 342.775 kNm**, agreeing with the closed-form solution within the test tolerance of 0.001%.
- **Original EC demo preserved:** shear utilisation 0.303499, bending 0.912166, deflection 0.609935; PASS for the implemented checks.

## Validation and reproduction

Run from the repository root with Node.js 22 or newer:

```text
node --test tests/*.test.cjs
```

**31 tests passed** at completion: 30 structural/behaviour regressions and one distribution-parity check. The suite includes simply supported UDL, cantilever tip load, fixed-fixed UDL, triangular load, applied couple, stable/unstable hinges, equilibrium, displacement, elastic Mcr, load-combination selection, classification, data identities, invalid inputs and the identified verdict gaps.

The section-data test checks mass/area, major elastic modulus and both radii of gyration for the PFC/UB/UC/RHS tables. These consistency identities are **not** an independent transcription check of every property or torsional constant.

Build the single-file application in PowerShell:

```text
./build-single-html.ps1
node --test tests/*.test.cjs
```

The distribution test requires exact source/CSS parity after newline normalisation. The build uses a literal replacement callback so JavaScript dollar sequences cannot be interpreted as regular-expression substitutions. GitHub Actions repeats the tests and verifies a reproducible build.

Browser checks covered the modular and standalone applications, the default EC report, the switch to BS factors, an unrestrained EC report and rejection of zero length. The default standalone report reproduced the modular numerical results.

## Design scope that remains

- PASS concerns only implemented member checks and the combinations explicitly enabled by the user. The application does not generate a complete project-specific action envelope, including pattern loading, uplift, leading-variable alternatives or accidental cases.
- Support bearing (the seating itself), connections, welds, global frame stability, fire, fatigue, vibration, lateral deflection and project-specific serviceability criteria require separate consideration. Web patch loading is now checked on the EC3 path (EN 1993-1-5 clause 6 + 7.2, "19 Sep 2026 gap closure, group G2" below); the BS 5950 path still carries only an advisory. A welded plate remains a geometry/self-weight feature, not a designed composite section or weld connection.
- The bending solver is linear Euler–Bernoulli with constant EI and ideal supports. It does not include shear deformation, support settlement or general geometric/material nonlinearity. Peaks are sampled on the analysis grids.
- The entered effective lengths and lateral/torsional restraints must represent the actual structure. The LTB solver assumes the stated ideal fork/clamp conditions and retained lateral/warping continuity at in-plane hinges.
- Open-section torsion is solved elastically for any support layout, load position and warping condition (P385 closed forms where they apply, the general warping-torsion FE elsewhere, "19 Sep 2026 gap closure, group G4" below). Second-order growth of load eccentricity and general nonlinear torsion are not solved. CF torsion and the combined torsion + N / M<sub>z</sub> interaction remain blocked.
- Class 4 effective-section design under a combined stress gradient and web shear-buckling resistance remain unimplemented and blocked where identified (the effective area of a Class-4 web in uniform compression is implemented, "19 Sep 2026 gap closure, group G3" below). Biaxial classification of channels and hollow sections and the member-interaction bounds may be conservative.
- The audit does not exhaustively validate every section, every restraint arrangement, every code provision or the legacy helper functions superseded by the active eigenvalue patch. A project-specific independent engineering check remains necessary.

## Reference basis

- [SCI P362, Steel Building Design: Concise Eurocodes](https://www.steelconstruction.info/images/6/6a/SCI_P362.pdf): classification/member resistance, UK NA LTB curve allocation in Table 6.6, and span-based serviceability guidance in Table 7.1.
- [SCI P363, Steel Building Design: Design Data](https://www.steelconstruction.info/images/b/b7/SCI_P363.pdf): hollow-section local-buckling dimensions and section-property reference.
- [SCI P385, Design of Steel Beams in Torsion](https://www.steelconstruction.info/images/6/6f/Sci_p385.pdf): covered torsion procedures and Appendix A section constants; Table A.7 identifies actual wall thicknesses.
- [SCI P351, The Bare Steel Design of Multi-storey Frames](https://www.steelconstruction.info/images/a/a5/SCI_P351.pdf): BS 5950 worked examples using 1.4G + 1.6Q.
- [SCI member-design guidance](https://steelconstruction.info/topics/design/member-design): scope of cross-section resistance, buckling and combined-action verification.

The normative clause references in the implementation remain subject to the selected standard edition and National Annex. This audit records engineering/software corrections, not professional certification or approval of a specific structure.

## 19 Sep 2026 gap closure

Gap group G1 of [docs/COVERAGE_MATRIX.md](docs/COVERAGE_MATRIX.md) section 5 (analysis level): items 1.3, 1.2, 3.9(b) and 3.19. Branch `ms-brief-standard-mcr`, on top of commit `0a2b016`. Regression tests: `tests/gap-closure.test.cjs` (10 tests, hand-computed expected values); the batch campaign (`tests/batch/run-batch.cjs`) gained an independent uplift cross-check (vii) and applies each pattern combination's own load mask when it re-derives the closed-form M<sub>cr</sub> inputs.

| Item | Was | Now |
|---|---|---|
| 1.3 Automatic pattern loading | Only user-entered combinations; a continuous or cantilevered beam with all its Q on one case never saw an adverse/relieving pattern | `S.autoPattern` (default true, checkbox under Load Combinations). `spanSegments()` splits the member at its supports (intervals between consecutive supports plus any cantilever/overhang beyond the outer supports; internal hinges do not split segments). When there is more than one segment, `expandPatternCombos()` follows every enabled ULS and SLS combination with a non-zero Q factor by the patterns "Q on span *i* only", "Q on spans *i*+*i*+1 only", "Q on odd spans only", "Q on even spans only" (patterns whose Q load set is empty, equals the parent's or repeats an earlier one are dropped), G/W/E at their entered factors on every span (EN 1990 6.10 as entered). Each generated combination is a shallow copy of its parent with `mask = {case:'Q', segIdx, segs}`; `comboLoadPieces(combo)` applies the mask (a distributed load straddling a boundary is split there with the trapezoid interpolated; a point load or couple belongs to the first segment containing it) and, while patterns are active, splits every distributed Q load at the interior boundaries for **every** combination so the solver x-grid stays identical (the boundaries are support positions). Every consumer reads the pieces: `comboLoads` (bending), the St Venant torsion FE and the P385 list, `stdZgFor`/`stdLoadShape`/`mcrSN006aFor`/`sn003aC1`/`mcrStandardFor` (standard route, now taking a combination object), `cmTableB3`, and the eigen patch's `unitLoadsFor` (LTB solved for every generated combination). The brief lists the generated cases under the loads and numbers cases by the expanded lists; the report prints them in the combination tables. **Limitation, printed as an advisory:** γ<sub>G,inf</sub> = 1.0 on relieving spans (Table A1.2(B)) is not generated |
| 1.2 Uplift / hold-down | Negative reactions printed with their sign, never tested | `analyse()` records every support reaction < −1 N in every ULS and SLS combination (`a.uplift`); `holdDownCheck(a)` prints, per lifting support, "Hold-down required: R = −x kN at support n (x = … m) (combination …)" with the worst ULS force (the SLS uplift is listed alongside). Blocking (`c.unsupported`) unless the new per-support checkbox "hold-down provided" (`S.supports[i].holdDown`) is ticked, then an advisory carrying the design force. **Interpretation adopted:** a support that lifts only in SLS combinations (the default NA 2.23 "variable actions only" case has no G, so its reaction is not an equilibrium state; the first campaign run showed 17 ordinary continuous beams blocked by it) is reported as an advisory "Hold-down check (SLS only) …" naming the combination and force and stating that the EQU set-A combination (γ<sub>G,inf</sub> = 0.9, Table A1.2(A)) is not generated; ULS uplift blocks. Both EC3 routes (through `checksEC3Restrained`) and the BS 5950 dispatcher carry it. Printed in the Member Forces block of the brief (NOT VERIFIED row / hold-down row / SLS-only advisory row / "Uplift … OK" line) and after the reactions line of the report |
| 3.9(b) Cantilever strut length | The cantilever preset left L<sub>E</sub> = 1.0 and the strut check used L<sub>cr</sub> = L | `annexB2()`: single fixed support with N<sub>Ed</sub> > 0 → K = 2.0 (fixed-free sway length, both axes) while `S.leFactor` is 1.0; any other entered factor is kept as an override. `buck.Ky/Kz/cantStrut/leOverride/lcrBasis` are printed in the brief (L<sub>ey</sub>/L<sub>ez</sub> lines, tag "cantilever 2.0L" / "user L<sub>E</sub>") and in the report |
| 3.19 Per-segment deflection limits | One divisor for every segment; a cantilever held to span/360 | Per segment: span/`S.divisor` between supports, L/`S.divisorCant` for a cantilever segment (default 180 per UK NA to EN 1993-1-1 Table NA.2 cantilever row, printed with [verify]), each capped by the optional absolute limit `S.deflAbs` (mm, blank = none). `a.deflection` carries `divisor, limit, limSpan, abs, absGoverns, cant`; `a.deflSegments` lists every segment with its worst SLS combination. Cantilever value = tip deflection relative to its support (root rotation inside the solver). Brief and report print the limit used per segment |

**Numerical evidence** (tests/gap-closure.test.cjs): two spans of 4 m, UDL Q: both loaded M<sub>support</sub> = wL²/8 = 60.00 kN·m (engine 60.00), one span loaded M<sub>sag</sub> = 0.0957wL² = 45.94 (engine 45.94), far reaction −wL/16 = −7.50 kN (engine −7.50); three spans of 5 m: all loaded 0.100wL² = 45.00 (engine 45.00), odd spans 0.1013wL² = 45.59 (engine 45.56), adjacent pair 0.1167wL² = 52.52 (engine 52.50); 6 m back span + 2 m overhang, UDL 10 + tip 30 kN (1.5Q): back-span-unloaded pattern R<sub>1</sub> = −(15×2×1 + 45×2)/6 = −20.00 kN (engine −20.00); cantilever 457×191×82, 4 m, N = 300 kN: λ̄<sub>y</sub> = 0.490 (2.0L) against 0.245 (L), λ̄<sub>y</sub> = 0.368 with the override 1.5; cantilever tip deflection PL³/3EI reproduced and limited to L/180 (22.22 mm for L = 4 m), L/250 → 16 mm, absolute 5 mm governs. With `autoPattern:false` (and `divisorCant:360`) the two-span, overhang-with-LTB and three-span standard-route cases reproduce the commit-0a2b016 figures to 1e-12.

**Batch campaign** after the change: see tests/batch/README.md (multi-span, overhang and Gerber cases now run 3–7 ULS combinations each; the new cross-check (vii) confirms that every support lifting in the governing combination is reported and, without a hold-down, blocking; SLS-only uplift of the Q-only NA 2.23 combination is advisory).

**Still open after G1:** γ<sub>G,inf</sub>/EQU set-A combinations are not generated (advisory only); W and E cases are not patterned; the standard M<sub>cr</sub> route still evaluates the whole member for the governing-moment combination only; the legacy `checksEC3()` at the top of `js/checks/eurocode-checks.js` and the non-SCI EC3 branch of `render()` remain unreachable (not touched by G1).

## 19 Sep 2026 gap closure, group G2: web transverse forces (EN 1993-1-5 clause 6 + 7.2)

Coverage-matrix items 2.16 and 2.18 ([docs/COVERAGE_MATRIX.md](docs/COVERAGE_MATRIX.md) section 5, rank 1, P1). Branch `ms-brief-standard-mcr`, on top of the G1 commit `c3adfbe`. Regression tests: `tests/web-transverse.test.cjs` (9 tests, expected values hand derived step by step in the test comments, marked [hand-derived]; no published worked example with matching inputs was at hand in-session); the batch campaign gained the independent cross-check (viii-FRd) that recomputes F<sub>Rd</sub> at the governing station of every rolled I/H run from the raw section table.

| Item | Was | Now |
|---|---|---|
| 2.16 Resistance of the web to transverse forces | One unconditional advisory (`eurocode-checks.js:304`), nothing computed: a beam with a heavy point load or a heavy end reaction on an unstiffened web could PASS | `webTransverseCheck(a, sec, fy, eps, cl)` in `js/checks/eurocode-checks.js`, called by `checksEC3Restrained` and therefore shared by both unrestrained routes (eigen patch and standard closed form). **Stations:** every point load with a non-zero P and every support; loads within 0.5 mm share a station (forces summed, the smaller s<sub>s</sub>), a point load at a support position is a "both" station. **Load types** (Figure 6.1): (a) interior load resisted by shear in the web, k<sub>F</sub> = 6 + 2(h<sub>w</sub>/a)²; (b) load transferred through the web to the opposite flange (point load over a support), k<sub>F</sub> = 3.5 + 2(h<sub>w</sub>/a)², F<sub>Ed</sub> = max(P, R) taken conservatively as the through-load; (c) end reaction / load near an unstiffened end, k<sub>F</sub> = 2 + 6(s<sub>s</sub> + c)/h<sub>w</sub> ≤ 6, evaluated together with type (a) whenever s<sub>s</sub> + c < 2h<sub>w</sub>/3 (the value at which k<sub>F</sub>(c) reaches the long-panel 6, i.e. the code's own end-zone boundary), the lower F<sub>Rd</sub> governing. **Chain:** F<sub>cr</sub> = 0.9k<sub>F</sub>Et<sub>w</sub>³/h<sub>w</sub> (6.4); m<sub>1</sub> = f<sub>yf</sub>b<sub>f</sub>/(f<sub>yw</sub>t<sub>w</sub>) with b<sub>f</sub> limited to 15εt<sub>f</sub> each side of the web (6.5(1)); m<sub>2</sub> = 0.02(h<sub>w</sub>/t<sub>f</sub>)² if λ̄<sub>F</sub> > 0.5 else 0, one re-evaluation with m<sub>2</sub> = 0 when the first pass gives λ̄<sub>F</sub> ≤ 0.5; l<sub>y</sub> = s<sub>s</sub> + 2t<sub>f</sub>(1 + √(m<sub>1</sub> + m<sub>2</sub>)) ≤ a for (a)/(b) (6.5(3)), min of the two 6.5(4) expressions with l<sub>e</sub> = k<sub>F</sub>Et<sub>w</sub>²/(2f<sub>yw</sub>h<sub>w</sub>) ≤ s<sub>s</sub> + c for (c); λ̄<sub>F</sub> = √(l<sub>y</sub>t<sub>w</sub>f<sub>yw</sub>/F<sub>cr</sub>), χ<sub>F</sub> = 0.5/λ̄<sub>F</sub> ≤ 1, L<sub>eff</sub> = χ<sub>F</sub>l<sub>y</sub>, F<sub>Rd</sub> = f<sub>yw</sub>L<sub>eff</sub>t<sub>w</sub>/γ<sub>M1</sub> (6.2), γ<sub>M1</sub> = 1.0 (UK NA), f<sub>yw</sub> = f<sub>yf</sub> = the flange-thickness f<sub>y</sub> (conservative for the thinner web). **Inputs:** per point load `ld.ss` (mm, blank = 0) and `ld.stiff`; per support `sp.ss` (blank = the section flange width B as a typical seating, printed with [verify]) and `sp.stiff`; s<sub>s</sub> ≤ h<sub>w</sub> (6.3(1)); c = max(d − s<sub>s</sub>/2, 0) with d the distance from the station to the nearer member end; a = the full member length when no stiffener is declared (conservative, printed), otherwise the panel between the declared stiffeners bounding the station. **Sections:** rolled I/H (h<sub>w</sub> = h − 2t<sub>f</sub>, b<sub>f</sub> = B ≤ t<sub>w</sub> + 30εt<sub>f</sub>), PFC (one web, one-sided flange b<sub>f</sub> = B ≤ t<sub>w</sub> + 15εt<sub>f</sub>), RHS/SHS (two webs, each a plate of thickness t with the tabulated flat depth d = h − 3t, flange share B/2 per web ≤ t + 15εt, the load shared between the webs by the lever rule of its eccentricity e, 0.5 each when e = 0). **F<sub>Ed</sub>** per station per ULS combination from that combination's own load pieces (pattern masks apply) and its own reactions (a lifting support bears nothing). **Verdict:** `c.utils` entries "Web transverse force  F_Ed/F_Rd (EN 1993-1-5 6.2)" (worst station) and the 7.2 entry below; a failing station is a FAIL with the advisory "Bearing stiffener required at x = … ". A station whose "bearing stiffener provided" box is ticked prints "stiffener declared - design stiffener separately (EN 1993-1-5 9.4)" as an advisory instead of the check |
| 2.18 Interaction 7.2 | Missing | η<sub>2</sub> + 0.8η<sub>1</sub> ≤ 1.4 at every station in every ULS combination, η<sub>2</sub> = F<sub>Ed</sub>/F<sub>Rd</sub>, η<sub>1</sub> = M<sub>Ed</sub>/M<sub>c,Rd</sub> (+ N<sub>Ed</sub>/N<sub>pl,Rd</sub> when N<sub>Ed</sub> ≠ 0) at the same station with the unreduced class-consistent M<sub>c,Rd</sub> = W<sub>y</sub>f<sub>y</sub>/γ<sub>M0</sub>; `c.utils` entry "Web transverse force + bending (EN 1993-1-5 7.2)" = (η<sub>2</sub> + 0.8η<sub>1</sub>)/1.4 of the worst station. **Interpretations, printed with [verify]:** EN 1993-1-5 4.6 writes η<sub>1</sub> with W<sub>eff</sub> (elastic for Class 3); W<sub>pl</sub> is used for Class 1/2 rolled sections as specified for this gap closure. Where the loaded flange is in tension (e.g. a cantilever tip load, an overhang), 7.2(2) refers to 6.2.1(5) of EN 1993-1-1 instead; the 7.2 expression is still applied as a screen and the row says so |

**Numerical evidence** (tests/web-transverse.test.cjs, all [hand-derived]): 457×191×82 S275, 6 m, 200 kN (Q) at mid-span, s<sub>s</sub> = 100 mm everywhere, 1.5Q: mid-span type (a) k<sub>F</sub> = 6.010, F<sub>cr</sub> = 2575.2 kN, l<sub>y</sub> = 317.59 mm, λ̄<sub>F</sub> = 0.579, χ<sub>F</sub> = 0.863, F<sub>Rd</sub> = 746.1 kN, F<sub>Ed</sub>/F<sub>Rd</sub> = 0.402, 7.2: 0.402 + 0.8 × 450/503.25 = 1.117 ≤ 1.4 (0.798); end reaction type (c) k<sub>F</sub> = 3.402, F<sub>cr</sub> = 1457.6 kN, l<sub>e</sub> = 100, l<sub>y</sub> = min(227.03, 192.79) = 192.79 mm, λ̄<sub>F</sub> = 0.600, F<sub>Rd</sub> = 437.3 kN (type (a) at the same station 746.1 kN), 150/437.3 = 0.343 (engine identical to 4 significant figures throughout). Demo beam end reaction with the default s<sub>s</sub> = B = 191.3: F<sub>Rd</sub> = 622.8 kN, 229.5/622.8 = 0.369 (demo shear/bending/deflection figures of the original audit unchanged). Point load with s<sub>s</sub> = 0: first pass λ̄<sub>F</sub> = 0.480 ≤ 0.5 → m<sub>2</sub> = 0, l<sub>y</sub> = 172.7 mm, χ<sub>F</sub> = 1, F<sub>Rd</sub> = 470.1 kN, so 750 kN at ULS on a 2 m 457×191×82 is FAIL (η<sub>2</sub> = 1.595) until the stiffener is declared. Point load over a support: type (b) F<sub>Rd</sub> = 533.1 kN and type (c) 355.8 kN, (c) governs with F<sub>Ed</sub> = max(150, 300) = 300 kN. RHS 200×100×8: per-web F<sub>Rd</sub> = 233.2 kN (type a, m<sub>2</sub> = 0 after the second pass, χ<sub>F</sub> = 1) and 154.0 kN (type c), 466.4 / 308.0 kN for a central load; e = 30 mm sends 0.826 of the load to the near web.

**Batch campaign** after the change: see tests/batch/README.md (cross-check viii-FRd on every rolled I/H run; verdict changes are the heavy point-load and heavy-reaction cases now failing the web check with s<sub>s</sub> = 0 at the loads and s<sub>s</sub> = B [verify] at the supports).

**Still open after G2:** the BS 5950 path keeps its cl 4.5.2 / 4.5.3 advisory (web bearing P<sub>bw</sub> and buckling P<sub>x</sub> not implemented); distributed loads are not treated as patch loads; loads hung from the bottom flange, the closely spaced total-load check (6.3(3)), flange-induced buckling (EN 1993-1-5 section 8) and the stiffener design (9.4) are not evaluated; the 7.2(2) tension-flange case is screened with the 7.2 expression rather than 6.2.1(5); the box-section flange contribution to m<sub>1</sub> (B/2 per web) and the lever-rule load share are engineering interpretations, not code text.

## 19 Sep 2026 gap closure, group G3: classification, effective area, M-V-N, k<sub>c</sub> floor, RHS k<sub>ij</sub>, channel torsional buckling, high-shear M<sub>v,Rd</sub>, restraint forces

Coverage-matrix P2 items 5, 6, 7, 8, 9, 10, 12 and 15 ([docs/COVERAGE_MATRIX.md](docs/COVERAGE_MATRIX.md) section 5). Branch `ms-brief-standard-mcr`, on top of the G2 commit `e2fcd00`. Regression tests: `tests/gap-closure-g3.test.cjs` (9 tests, expected values hand derived in the test comments and marked [hand-derived]; every FE-supplied station force is fed back through the code expression and the raw section table). The batch campaign gained the independent cross-checks (ix-Aeff), (x-NbT), (xi-kcFloor) and (xii-MVN) and four cases (UB-46, UB-47, UB-48, PFC-20). All new checks enter the verdict (`c.utils`), print in the MasterSeries brief in their MasterSeries position and in the detailed report, and every removed block is listed in the last row.

| Item | Was | Now |
|---|---|---|
| 5 (1.9(b)) Minor-axis classification of I/H | Any M<sub>z</sub> ≠ 0 applied the uniform-compression web bound 33ε/38ε/42ε to every section; most UBs (d/t<sub>w</sub> > 38.8 at S275) became Class 4 and every biaxial UB/UC case was NOT VERIFIED | `classifyEC3` (js/01-computation-engine.js): for `sec.kind === 'I'` the web keeps its y-y bending (+N) limits, because M<sub>z</sub> does not stress a web lying on the z-z axis (`cl.mzFlange = 'outstand'`, `webCase` unchanged). The flanges are outstands under the combined stress: `mzFlangeStress()` (eurocode-checks.js) evaluates σ = N/A + M<sub>y</sub>/W<sub>el,y</sub> ± M<sub>z</sub>y/I<sub>z</sub> at the root (y = t<sub>w</sub>/2 + r) and tip (y = B/2) of both outstands of the compression flange; the outstand compressed by M<sub>z</sub> has both values compressive (α = 1), for which Table 5.2 sheet 2 gives 9ε/α, 10ε/α and 21ε√k<sub>σ</sub> with k<sub>σ</sub> ≥ 0.43 (EN 1993-1-5 Table 4.2), never stricter than the uniform-compression bound 9ε/10ε/14ε already applied, which is kept; the opposite outstand (tip relieved, in tension when σ<sub>Mz,tip</sub> > σ<sub>N</sub> + σ<sub>My</sub>) takes the laxer gradient limits and never governs. Channels and hollow sections keep the conservative uniform-compression bound (a wall parallel to the web is compressed over its full width by M<sub>z</sub>). A 457×191×82 with M<sub>z</sub> = 60 kN·m is Class 1 and its biaxial check (M<sub>y</sub>/M<sub>N,y</sub>)² + M<sub>z</sub>/M<sub>N,z</sub> = 1.550 is a real FAIL; with M<sub>z</sub> = 10 kN·m it PASSes at 0.951. Brief: "Web classified for" row states the basis, new row "Flange outstands under M<sub>y</sub> + M<sub>z</sub> (+N)" with the four stresses (tag Table 5.2 sheet 2); report: the same in the classification block |
| 6 (1.9(c) / 3.9(c)) A<sub>eff</sub> for a Class-4 web in uniform compression | Any I/H with d/t<sub>w</sub> > 42ε and N > 0 blocked ("Effective-area compression buckling resistance is not implemented"), e.g. every 457×191×82 beam-column | `aeffWebCompression(sec, eps)` (EN 1993-1-5 4.4, internal element, ψ = 1, k<sub>σ</sub> = 4): λ̄<sub>p</sub> = (b̄/t)/(28.4ε√k<sub>σ</sub>) with b̄ = the Table 5.2 flat depth `sec.d`, ρ = (λ̄<sub>p</sub> − 0.055(3 + ψ))/λ̄<sub>p</sub>² ≤ 1 (1 for λ̄<sub>p</sub> ≤ 0.673), b<sub>eff</sub> = ρb̄ split b<sub>e1</sub> = b<sub>e2</sub> = 0.5b<sub>eff</sub>, A<sub>eff</sub> = A − n<sub>webs</sub>(1 − ρ)b̄t (n<sub>webs</sub> = 2 for a hollow section), e<sub>N</sub> = 0 (symmetric). Applied when N > 0 and d/t > 42ε: N<sub>c,Rd</sub> = A<sub>eff</sub>f<sub>y</sub>/γ<sub>M0</sub> (6.2.4(2)) in the verdict entry "Compression N<sub>Ed</sub>/N<sub>c,Rd</sub> (A<sub>eff</sub>)"; `annexB2`: λ̄ = √(A<sub>eff</sub>f<sub>y</sub>/N<sub>cr</sub>) (6.3.1.3(1), i.e. ×√(A<sub>eff</sub>/A)), N<sub>b,Rd</sub> = χA<sub>eff</sub>f<sub>y</sub>/γ<sub>M1</sub>, Eq 6.61/6.62 with the Table 6.7 Class-4 column: N<sub>Rk</sub> = A<sub>eff</sub>f<sub>y</sub>, M<sub>y,Rk</sub> = W<sub>eff,y</sub>f<sub>y</sub> with **W<sub>eff,y</sub> = W<sub>el,y</sub>** (flanges Class ≤ 3 in compression, web Class 4 only in uniform compression; a Class 1/2 M<sub>b,Rd</sub> handed in is scaled by W<sub>el,y</sub>/W<sub>pl,y</sub>, `buck.wFac`), ΔM = 0, k<sub>ij</sub> from the Class 3/4 rows (`c12 = false`), M<sub>c,z,Rd</sub> = W<sub>el,z</sub>f<sub>y</sub>. **Assumption printed** in the brief (Classification row "A<sub>eff</sub> = A − (1 − ρ)b̄t<sub>w</sub>", tag EN 1993-1-5 4.4; Local Capacity rows N<sub>c,Rd</sub> and N<sub>Ed</sub>/N<sub>c,Rd</sub>; Compression Resistance row "A<sub>eff</sub>"; Buckling Resistance row "Table 6.7 Class-4 column") and in the report. The cross-section interaction under the combined actions keeps the class found under the actual stress distribution (5.5.2: parts in compression under the load combination considered), so a 457×191×82 with N = 600 kN is Class 2 (plastic M<sub>N,Rd</sub>) for the cross-section and takes A<sub>eff</sub> for the compression and member terms. Flange Class 4 in uniform compression (c/t > 14ε outstand, > 42ε internal) and a channel web Class 4 in compression (S460 only; the reduction would shift the centroid, N<sub>Ed</sub>e<sub>N</sub>) remain blocked with explicit messages; a web Class 4 **under the combined N + M stress gradient** (e.g. 1016×305×249 with N = 6000 kN) remains blocked because the ψ-dependent effective width with its e<sub>N</sub> shift is not implemented |
| 7 (2.13) Bending, shear and axial together, 6.2.10 | Any station with V > 0.5V<sub>pl,Rd</sub> and N ≠ 0 blocked ("combined cl 6.2.10 reduction is not implemented"); M<sub>z</sub> with high shear blocked separately | Sweep of every station of every ULS combination's own (V, M) fields (the pattern combinations included). Where V<sub>Ed</sub> > 0.5V<sub>pl(,T),Rd</sub>: ρ = (2V<sub>Ed</sub>/V<sub>pl,Rd</sub> − 1)² and `shearReducedResistances()` gives the section with (1 − ρ)f<sub>y</sub> on the shear area: N<sub>V,Rd</sub> = (A − ρA<sub>v</sub>)f<sub>y</sub>/γ<sub>M0</sub>, M<sub>v,y,Rd</sub>, M<sub>v,z,Rd</sub> (forms in item 12) and the 6.2.9.1 parameter evaluated on the reduced-yield section a<sub>V</sub> = ((A − 2bt<sub>f</sub>) − ρA<sub>v</sub>)/(A − ρA<sub>v</sub>) ≤ 0.5 (smaller than the gross a, hence conservative) and a<sub>f,V</sub> for hollow sections. I/H Class 1/2: M<sub>N,V,y,Rd</sub> = M<sub>v,y,Rd</sub>(1 − n<sub>V</sub>)/(1 − 0.5a<sub>V</sub>) ≤ M<sub>v,y,Rd</sub> with n<sub>V</sub> = N<sub>Ed</sub>/N<sub>V,Rd</sub> and the 6.2.9.1(4) waiver re-written with the reduced values (N<sub>Ed</sub> ≤ 0.25N<sub>V,Rd</sub> and N<sub>Ed</sub> ≤ 0.5h<sub>w</sub>t<sub>w</sub>(1 − ρ)f<sub>y</sub>/γ<sub>M0</sub>), M<sub>N,V,z,Rd</sub> per 6.2.9.1(5); RHS/SHS: Eq 6.39/6.40 with a<sub>w,V</sub>, a<sub>f,V</sub>; Class 3 and channels: N<sub>Ed</sub>/N<sub>V,Rd</sub> + M<sub>y</sub>/M<sub>v,y,Rd</sub> + M<sub>z</sub>/M<sub>v,z,Rd</sub>. Utilisation: **uniaxial M<sub>y</sub> + N: the plain ratio M<sub>y,Ed</sub>/M<sub>N,V,y,Rd</sub>** (the α/β exponents of 6.2.9.1(6) apply to biaxial bending only); biaxial: (M<sub>y</sub>/M<sub>N,V,y</sub>)<sup>α</sup> + (M<sub>z</sub>/M<sub>N,V,z</sub>)<sup>β</sup> with α = 2, β = max(5n<sub>V</sub>, 1) (I/H) or α = β = 1.66/(1 − 1.13n<sub>V</sub>²) (RHS). Verdict entry "Bending+shear+axial (6.2.10)" / "…+biaxial (6.2.10)" for the worst station (`c.mvn`); a station with V<sub>Ed</sub> > V<sub>pl,Rd</sub> is a pure shear failure and no 6.2.10 value is formed there. Brief: row in the Local Capacity block after the coexistent M-V row with the full chain (tag 6.2.10) and unity cell "M-V-N"; report: row in Moment Resistance with a note. The two blocks (N and M<sub>z</sub>) are removed |
| 8 (3.5) k<sub>c</sub> floor | k<sub>c</sub> = 1/√C<sub>1</sub> with the back-calculated eigen C<sub>1</sub> unbounded (Gerber probe C<sub>1</sub> = 5.0 → k<sub>c</sub> 0.45); the Serna C<sub>1</sub> of the standard route can reach √35 = 5.9 | `kcFromC1()` (eurocode-checks.js): k<sub>c</sub> = max(min(1/√C<sub>1</sub>, 1), 1/√2.76) = 0.602 floor, the lower bound of Table 6.6 (ψ = −1 end-moment row, C<sub>1</sub> = 2.76); used by the eigen route (`chiChainFor`), the standard route (I/H, box and channel M<sub>cr</sub> chains) and the brief helper `msbKc`. `ltb.kcRaw`, `ltb.kcFloored`; the eigen route adds a warning line, the report prints "1/√C<sub>1</sub> = 0.447 → floored at 1/√2.76 = 0.602", the brief tags the χ<sub>LT,mod</sub> line "k<sub>c</sub> floored at 0.60 (Table 6.6)". The SCI end-moment curve at ψ = −1 gives C<sub>1</sub> = (1.33 + 0.33)² = 2.756 < 2.76 and is not floored (raw k<sub>c</sub> = 0.6024). Re-baselined figure: the three-span trapezoid of `tests/gap-closure.test.cjs` (Serna C<sub>1</sub> = 4.09) now has f = 0.810, χ<sub>LT,mod</sub> = 0.828, M<sub>b,Rd</sub> = 416.7 kN·m (was 444.9 with k<sub>c</sub> = 0.494) |
| 9 (3.12(a)) Table B.1 RHS row | k<sub>zz</sub>/k<sub>yz</sub> for RHS/SHS used the I-section row | `annexB2`: for `sec.isBox` Class 1/2, k<sub>zz</sub> = C<sub>mz</sub>[1 + (λ̄<sub>z</sub> − 0.2)n<sub>z</sub>] ≤ C<sub>mz</sub>(1 + 0.8n<sub>z</sub>) and k<sub>yz</sub> = k<sub>zz</sub> (`buck.rhsRow`); Class 3: k<sub>zz</sub> = C<sub>mz</sub>(1 + 0.6λ̄<sub>z</sub>n<sub>z</sub>) ≤ C<sub>mz</sub>(1 + 0.6n<sub>z</sub>), k<sub>yz</sub> = k<sub>zz</sub> (unchanged form, k<sub>yz</sub> already k<sub>zz</sub>). Brief rows tagged "Table B.1 (RHS)"; report names the row. SHS 150×150×6.3, 6 m, N = 200 kN, M<sub>z</sub> = 5: k<sub>zz</sub> = min(1.368, 1.300) = 1.300 (was 1.404 → 1.525 cap on the I row) |
| 10 (3.10) PFC torsional and torsional-flexural buckling | Any channel with N > 0 blocked ("cl 6.3.1.4 not implemented") | `torsionalFlexuralBuckling()` in `annexB2` for a channel with N<sub>Ed</sub> > 0: y<sub>0</sub> = e<sub>sc</sub> (shear centre to centroid, `sec.tp.esc`, SCI P385 Table A.3 = Blue Book e<sub>0</sub> + c<sub>y</sub>, source printed), i<sub>0</sub>² = i<sub>y</sub>² + i<sub>z</sub>² + y<sub>0</sub>², N<sub>cr,T</sub> = (GI<sub>T</sub> + π²EI<sub>w</sub>/L<sub>T</sub>²)/i<sub>0</sub>² with I<sub>T</sub>, I<sub>w</sub> from P385 Table A.3 (section table fallback), G = 81000, L<sub>T</sub> = L<sub>cr,z</sub> unless the new input `S.LT` (m, "Torsional buckling length L<sub>T</sub>", shown for PFC on the EC3 path, validated > 0) is entered; N<sub>cr,TF</sub> = (N<sub>cr,y</sub> + N<sub>cr,T</sub>)/(2β)[1 − √(1 − 4βN<sub>cr,y</sub>N<sub>cr,T</sub>/(N<sub>cr,y</sub> + N<sub>cr,T</sub>)²)], β = 1 − (y<sub>0</sub>/i<sub>0</sub>)², where **N<sub>cr,y</sub> is the flexural load about the axis of symmetry y-y (the major axis, L<sub>cr,y</sub>)** — the shear-centre offset of a channel lies on the y-y axis, so twist couples with major-axis flexure and the minor-axis mode stays pure (EN 1993-1-3 6.2.3(6) form; Timoshenko); N<sub>cr</sub> = min(N<sub>cr,T</sub>, N<sub>cr,TF</sub>), λ̄<sub>T</sub> = √(Af<sub>y</sub>/N<sub>cr</sub>), χ<sub>T</sub> on the curve related to the z-z axis (6.3.1.4(3)): **Table 6.2 "U-sections: any axis → curve c"** is used, not the "curve b [verify]" of the gap brief, since Table 6.2 lists channels under c (printed [verify]); N<sub>b,T,Rd</sub> = χ<sub>T</sub>Af<sub>y</sub>/γ<sub>M1</sub>. Verdict entry "Torsional-flexural buckling N<sub>Ed</sub>/N<sub>b,T,Rd</sub> (6.3.1.4)" in all three EC3 routes; the lower of χ<sub>T</sub> and the flexural χ feeds both axial terms of Eq 6.61/6.62 (`buck.NbYeff`, `buck.NbZeff`) as a conservative extension of the doubly-symmetric Annex B forms. Brief: six rows in Compression Resistance (i<sub>0</sub>², N<sub>cr,T</sub>, N<sub>cr,TF</sub>, λ̄<sub>T</sub>, N<sub>b,T,Rd</sub> "Curve c", N<sub>Ed</sub>/N<sub>b,T,Rd</sub>), unity cell "N<sub>b.T</sub>"; report: row in Member Buckling Resistance with a note. The block is removed; a section without tabulated e<sub>sc</sub>/I<sub>T</sub> still blocks with a message |
| 12 (2.10) High-shear M<sub>v,Rd</sub> for every family | Only rolled I/H Class 1/2 had M<sub>v,Rd</sub>; Class 3 I/H, channels and hollow sections blocked at the peak-moment station and in the span-wise sweep | `shearReducedResistances(sec, cl, fy, A_v, ρ)`: I/H Class 1/2 (W<sub>pl,y</sub> − ρA<sub>v</sub>²/4t<sub>w</sub>)f<sub>y</sub> (unchanged); **I/H Class 3** (W<sub>el,y</sub> − ρI<sub>web</sub>/(h/2))f<sub>y</sub> with I<sub>web</sub> = t<sub>w</sub>h<sub>w</sub>³/12 (web fibre stress limited to (1 − ρ)f<sub>y</sub>, printed as conservative); **channels** (W<sub>pl,y</sub> or W<sub>el,y</sub> − ρt<sub>w</sub>h<sub>w</sub>²/4)f<sub>y</sub> (plastic web modulus, conservative for Class 3); **RHS/SHS** (W<sub>pl,y</sub> or W<sub>el,y</sub> − ρt(h − 2t)²/2)f<sub>y</sub> (two webs). Minor axis for the 6.2.10 forms: I/H (W<sub>pl,z</sub> − ρA<sub>v</sub>t<sub>w</sub>/4)f<sub>y</sub> / (W<sub>el,z</sub> − ρA<sub>v</sub>t<sub>w</sub>/6)f<sub>y</sub>, channel M<sub>c,z,Rd</sub>(1 − ρA<sub>v</sub>/A) (web share bounded by its area share, conservative), RHS (W<sub>z</sub> − ρt(h − 2t)(b − t))f<sub>y</sub>. Every value ≤ its unreduced counterpart. The peak-moment reduction of M<sub>c,Rd</sub> (`c.McRd`, `c.mvForm`, `c.rhoAtM`) and the span-wise sweep (`c.coex`) now run for every family and Class 1–3; the brief prints the family form on the "M<sub>v.y.Rd</sub> = …" line and the coexistent row; the three blocks are removed |
| 15 (1.7 / 3.17) Restraint design forces | Nothing computed for any restraint | `restraintForces(a, sec)` on the not-fully-restrained path: for every support (torsional restraint) and every intermediate lateral restraint, M<sub>Ed</sub> = the largest |M| at the station over the analysed ULS combinations (patterns included, read a fraction inside the station), N<sub>f,Ed</sub> = M<sub>Ed</sub>/h with h the overall depth (EN 1993-1-1 5.3.3(3) notation), design force 2.5 % N<sub>f,Ed</sub> (6.3.5.2(5)(b) / SCI practice). Advisory: not in `c.utils`. Brief: table in the "Lateral Restraint Portions" block (the block is printed on every LTB path, headed "(span by span, fork ends)" with the portion table when segments exist, "(restraint design forces)" otherwise), every row tagged "restraint design force, advisory", with the basis note (a simply supported end with M<sub>Ed</sub> = 0 gets no flange force; its fork restraint must still prevent twist; the 5.3.3 stiffness/imperfection requirements apply to the bracing system); report: block "Restraint Design Forces" after the LTB block |
| Removed blocks | — | "The web is Class 4 in uniform compression…" (A<sub>eff</sub>), "High shear coincident with axial force anywhere…6.2.10", "High shear with minor-axis/biaxial bending…", "High shear coincident with the maximum moment…not implemented", "High shear and bending coexist away from…not implemented", "PFC under axial compression: torsional and torsional-flexural buckling…not implemented", and the biaxial uniform-compression advisory for I/H. Still blocking, with the reason printed: Class 4 under the combined stress gradient, flange Class 4 in compression, channel web Class 4 in compression, channel without tabulated e<sub>sc</sub> |

**Numerical evidence** (tests/gap-closure-g3.test.cjs, all [hand-derived]): 1016×305×249 S275 (f<sub>y</sub> = 265, ε = 0.9417), N = 3000 kN: λ̄<sub>p</sub> = 52.6/(28.4 × 0.9417 × 2) = 0.9834, ρ = 0.7894, b<sub>ineff</sub> = 182.8 mm, A<sub>eff</sub> = 31700 − 182.8 × 16.5 = 28683 mm² (0.905A), N<sub>c,Rd</sub> = 7601 kN, N<sub>Ed</sub>/N<sub>c,Rd</sub> = 0.395, λ̄<sub>y</sub> = (12000/390)/88.45 × 0.9512 = 0.331 (engine identical; the case evaluates, Eq 6.62 = 2.43 FAIL on 12 m unrestrained about z-z instead of NOT VERIFIED); 457×191×82: A<sub>eff</sub> = 10065.6 mm² (ρ = 0.9171). PFC 180×75×20 S275, 4 m, N = 50 kN: i<sub>0</sub>² = 72.7² + 23.8² + 49.6² = 8311.9 mm², N<sub>cr,T</sub> = (81000 × 7.6 × 10⁴ + π² × 210000 × 7.54 × 10⁹/4000²)/8311.9 = 858.1 kN, N<sub>cr,y</sub> = 1774.7 kN, β = 0.704, N<sub>cr,TF</sub> = 1869.9 × [1 − √0.3813] = 715.2 kN (TF mode), λ̄<sub>T</sub> = √(2590 × 275/715224) = 0.998, curve c: Φ = 1.193, χ<sub>T</sub> = 0.541, N<sub>b,T,Rd</sub> = 385.4 kN, N<sub>Ed</sub>/N<sub>b,T,Rd</sub> = 0.130; with L<sub>T</sub> = 2 m N<sub>cr,T</sub> = 1210.7 kN. 457×191×82, 2 m, 400 kN at 0.3 m, N = 500 kN: at x = 0.3 m V = 510.8 kN, ρ = 0.123, N<sub>V,Rd</sub> = (10400 − 0.123 × 4763) × 0.275 = 2698.8 kN, M<sub>v,y,Rd</sub> = 483.9 kN·m, a<sub>V</sub> = 0.376, waiver (500 ≤ 674.7 and ≤ 511) → M<sub>N,V,y,Rd</sub> = 483.9, M/M<sub>N,V,y,Rd</sub> = 153.3/483.9 = 0.317; N = 600 kN: no waiver, Class 2 under the combined stress (α = 0.770). SHS 150×150×6.3, N = 200 kN: λ̄<sub>z</sub> = 1.181, n<sub>z</sub> = 0.375, k<sub>zz</sub> = min(1.368, 1.300) = 1.300 = k<sub>yz</sub>. Gerber layout (fixed 0 / pins 8, 12 m, hinge 4 m): eigen C<sub>1</sub> = 4.998 → k<sub>c</sub> 0.447 → 0.602; restraint forces h = 460 mm: support 1 M<sub>Ed</sub> = 573.8 kN·m → N<sub>f,Ed</sub> = 1247.4 kN → 31.2 kN, support 2 344.3 → 18.7 kN, support 3 0. 457×191×82 with M<sub>z</sub> = 60 kN·m: σ<sub>My</sub> = 285.1, σ<sub>Mz,tip</sub> = 306.1, σ<sub>Mz,root</sub> = 48.5 N/mm² (y<sub>root</sub> = 15.15, y<sub>tip</sub> = 95.65), compressed outstand 333.6/591.2 (α = 1), relieved outstand tip −21.0 (tension). Demo beam figures of the original audit unchanged.

**Batch campaign** after the change: see tests/batch/README.md (118 cases / 214 runs; cross-checks ix–xii; the PFC axial cases and the biaxial UB cases now carry verdicts).

**Still open after G3:** the effective section under a combined N + M stress gradient (Class 4 web by the α/ψ limits, e<sub>N</sub> shift) and Class-4 flanges; the A<sub>eff</sub> treatment of a channel web (e<sub>N</sub>); the a<sub>V</sub> parameter and the minor-axis web deductions of the 6.2.10 forms are engineering interpretations of "(1 − ρ)f<sub>y</sub> on the shear area", printed as such; the restraint forces use h = overall depth (5.3.3(3) notation; the flange-centroid lever h − t<sub>f</sub> would give ×h/(h − t<sub>f</sub>)); the existing "Local" 6.2.9 entry keeps MasterSeries' squared form (M<sub>y</sub>/M<sub>N,y</sub>)² for the uniaxial case (verdict unaffected; the new 6.2.10 entry uses the plain ratio); the BS 5950 path is untouched by G3.

## 19 Sep 2026 gap closure, group G4: general warping-torsion FE for open sections

Coverage-matrix P2 item 11 (trigger 2.6, [docs/COVERAGE_MATRIX.md](docs/COVERAGE_MATRIX.md) section 5). Branch `ms-brief-standard-mcr`, on top of the G3 commit `b1dc961`. Regression tests: `tests/torsion-fe.test.cjs` (12 tests; expected values hand derived from the section constants and the classical closed forms of the Vlasov equation, marked [hand-derived], with the P385 Appendix C forms of `p385Solve` as the independent reference for the fork-fork cases). The batch campaign gained the cross-check (xiii-torsionFE) and four cases (UB-49, UB-50, UB-51, PFC-21).

| Item | Was | Now |
|---|---|---|
| 2.6 Torsion outside the P385 closed forms | `analyse()` built the open-section warping solution only from the P385 Appendix C closed forms (Cases 3/4/10, `p385Solve`), gated to one fork-fork span with full-span distributed torque; a cantilever, a multi-span or overhang layout, a partial-span eccentric distributed load or any warping restraint produced "Torsion on this open section is NOT COVERED" and, on a channel, the eigen route's "Channel with eccentric load … Annex A … not available" block (PFC-17 of the batch, the coverage-matrix probe 457×191×82 with a partial eccentric UDL) | `js/checks/torsion-fe.js` (new file, registered in index.html, build-single-html.ps1 and tests/harness.cjs): `warpingTorsionFE({L, EIw, GIt, supports, torques})` solves E I<sub>w</sub> φ⁗ − G I<sub>T</sub> φ″ = m<sub>t</sub>(x) with two-node elements carrying (φ, φ′) per node, K<sub>e</sub> = kBend(E I<sub>w</sub>, L<sub>e</sub>) + kTors(G I<sub>T</sub>, L<sub>e</sub>) — the Hermite matrices of the LTB eigensolver, duplicated as the pure functions `torsionFeKBend` / `torsionFeKTors` because the originals are private to the closure of js/08-mcr-eigen-patch.js — and the consistent nodal torques L<sub>e</sub>(7t<sub>a</sub> + 3t<sub>b</sub>)/20, L<sub>e</sub>²(3t<sub>a</sub> + 2t<sub>b</sub>)/60, … of a linearly varying torque. **Boundary conditions:** every support φ = 0 (fork / torsional restraint, as P385 assumes at every support); warping free (φ″ = 0, natural) by default, matching P385 fork ends; warping fixed (φ′ = 0 imposed) through the new per-support checkbox "warping restrained for torsion" (`S.supports[i].warpFix`, shown on the EC3 path when torsion is active on an open section) and automatically at the root of a cantilever (single fixed support); a free end / tip is natural (T = 0 and B = 0 come out of the assembled equilibrium exactly); twist and warping are continuous through an in-plane hinge (a bending release does not release twist; printed). **Loads:** the same torque list as the St Venant diagram (`mkT`, hoisted): point torques P·e [N·mm], distributed torques w·e [N·mm/mm] including trapezoids and the PFC self-weight eccentricity, per combination with the pattern masks; applied moments carry no torque; the per-load z<sub>g</sub> is a load height, not a torque. **Recovery:** φ and φ′ from the nodal DOFs; B = E I<sub>w</sub> φ″ and the total torque T = G I<sub>T</sub> φ′ − E I<sub>w</sub> φ‴ from the element end forces f = K<sub>e</sub>d<sub>e</sub> − f<sub>eq</sub> (the consistent flux, which satisfies B = 0 at fork and free ends exactly; B averaged at interior nodes; a node where T jumps — point torque, interior support — is reported as three stations x − 0.01, x, x + 0.01 mm like `p385Solve`); φ‴ = (G I<sub>T</sub> φ′ − T)/(E I<sub>w</sub>). The result has the shape of `p385Solve` ({xs, phi, p1, p2, p3, X}) so the P385 3.1.2 cross-section check, the V<sub>pl,T,Rd</sub> sweep, the rotation-induced M<sub>z</sub> = φM<sub>y</sub> and the EN 1993-6 Annex A interaction consume it unchanged; it also carries T, B, the support reactions (torque, and bimoment where warping is fixed), `nElem`, `nElemCoarse`, `meshError`, `converged`, `bc` and `methodLabel`. **Mesh convergence:** the base mesh (`TORSION_FE_NSUB` = 120 subdivisions plus every load and support point) is doubled once; the fine solution is reported with `meshError` = the largest relative change of max|φ|, max|φ′| and max|B|; above `TORSION_FE_MESH_BLOCK` = 0.5 % the check pushes "Warping-torsion FE mesh has not converged …" (blocking, printed in the Torsion Design block). **Routing** (`analyse()`): the closed forms stay the default where they apply (single span with supports at both ends, no warping-fixed support, full-span distributed torques and/or point torques in every analysed combination, patterns included; `torsO.method = 'closed'`); otherwise the FE (`torsO.method = 'fe'`, `feReasons` = "cantilever" / "n supports (multi-span / overhang layout)" / "warping-fixed support" / "partial-span eccentric distributed load …"). Both routes expose `method`, `methodLabel`, `bcText`; the check object carries `tor.method`, `tor.fe`, `tor.nElem`, `tor.meshError`, `tor.meshConverged`, `tor.feReasons`, `tor.BMax` (kN·m²), `tor.BMaxPos`, `tor.TEnds` (total torque at the ends, both routes) beside the existing `tor.TtEnds` (St Venant part). **Brief** (Torsion Design block, MasterSeries position): new first row "Torsion analysis" (closed: "SCI P385 App C closed forms (Cases 3/4/10); fork supports … | L/a | P385 App C"; FE: the equation, "warping-torsion FE (n elements)", the boundary conditions, "closed forms not applicable: …" | "mesh error x %" | "FE (≤ 0.5 %)" or BLOCKED), the φ<sub>max</sub> row names the method, new row "B<sub>Ed</sub> = EI<sub>w</sub>φ″ (max)" (kN·m²) before M<sub>w.Ed</sub>, the "End torques T<sub>t</sub>" row prints the St Venant part and the total torque T = GI<sub>T</sub>φ′ − EI<sub>w</sub>φ‴ at both ends. **Report:** the Torsion Analysis heading names the method, a "Method" line with the boundary conditions, the reasons and the mesh statement (OK / mesh NOT converged), a "Bimoment B<sub>Ed</sub>" line, the end-torque line with the total torque, and the P385 note names the FE model. The two blocking reasons "partial-span eccentric distributed load …" and "… cantilevers and multi-span layouts are not covered" are gone; "no warping constant available" and "I<sub>T</sub> zero or undefined" still block |

**Numerical evidence** (tests/torsion-fe.test.cjs, 457×191×82: I<sub>T</sub> = 69.2 cm⁴, I<sub>w</sub> = 0.922 dm⁶, G I<sub>T</sub> = 5.6052×10¹⁰ N·mm², E I<sub>w</sub> = 1.9362×10¹⁷ N·mm⁴, a = 1858.57 mm): (1) fork-fork 8 m, uniform torque 20 kN·m total (t = 2500 N·mm/mm): the FE reproduces `p385Solve` Case 4 to 1×10⁻⁴ or better in max φ, φ′, φ″, φ‴ and at the quarter points (requirement 0.5 %); φ(L/2) = (t a²/GI<sub>T</sub>)[L²/(8a²) + 1/cosh(L/2a) − 1] = 0.15407 × [2.3159 + 0.22944 − 1] = 0.2381 rad (engine 0.23808); B = 0 at both fork ends, total end torques ±10 kN·m, reactions sum to −20 kN·m; Case 3 (point torque at 0.3L) and Case 10 (rising linear torque) reproduced within 0.5 % including the φ‴ jump T/(E I<sub>w</sub>) across the point torque. (2) 4 m cantilever, tip torque 10 kN·m: φ(L) = (T/GI<sub>T</sub>)[L − a tanh(L/a)] = 1.7841×10⁻⁴ × (4000 − 1808.96) = 0.39090 rad (engine 0.390882, 2×10⁻⁸); root bimoment B(0) = T a tanh(L/a) = 1.80896×10¹⁰ N·mm² (engine identical to 4 figures); tip T = 10 kN·m, B = 0; root φ = φ′ = 0. In-app: 20 kN at e = 80 mm gives T = 2.4 kN·m, St Venant part at the root 0 and at the free tip T(1 − 1/cosh(L/a)) = 1.849 kN·m, total 2.4 kN·m at both stations. (3) warping fixed at both ends, uniform torque: φ(L/2) = (t/GI<sub>T</sub>)[L²/8 − (La/2) tanh(L/4a)] = 4.4602×10⁻⁸ × (8×10⁶ − 5.8865×10⁶) = 0.09427 rad (engine 0.094284) against 0.2381 with fork ends; φ′ = 0 at both ends, antisymmetric reaction bimoments equal to the recovered end values; one fixed end gives a value between the two. (4) mesh convergence on the Case 3 layout: single meshes of 3, 5, 9, 17 elements give errors at the torque station of 3.0×10⁻³, 8.4×10⁻⁴, 4.2×10⁻⁵, 2.3×10⁻⁶ (an order of magnitude per doubling once the element is shorter than a — the O(h⁴) nodal superconvergence of the Hermite element), the reported doubling error is 6.9 % on the 5 → 9 element mesh (would be blocked) and 2.8×10⁻⁸ on the default 120 → 240 mesh. (5) partial-span eccentric UDL 2–6 m at e = 100 mm on the 8 m UB (the coverage-matrix probe): the FE peak twist, φ′ and φ″ agree within 0.5 % with the superposition of 400 P385 Case 3 point torques (midpoint rule), the case PASSes with Annex A 0.73 and the brief / report print the method rows. (6) the fork-fork full-span PFC 180×75×20 case keeps `method = 'closed'` and the FE fed with the same torque list agrees with the closed forms within 0.5 % on every field; the demo, brief and structural fixtures are unchanged.

**Batch campaign** after the change: see tests/batch/README.md (122 cases / 222 runs; cross-check xiii-torsionFE 4/4; PFC-17 now carries a verdict on both routes — eigen PASS with Annex A 0.86, standard FAIL on the whole-member channel κ chain — and the trigger-mismatch table is empty; UB-49/50/51 and PFC-21 exercise the cantilever, partial-span, warping-fixed and multi-span routes; every other run unchanged to the printed digit).

**Still open after G4:** the FE is linear elastic (no second-order growth of the eccentricity with the twist, no plastic redistribution — as before for the closed forms); the twist restraint at every support is assumed (a support that does not prevent twist is not modelled); the LTB-side "warping φ′ fixed (LTB)" checkbox and the torsion-side "warping restrained for torsion" checkbox are separate inputs (the same physical detail should tick both); the total torque printed with the T<sub>Ed</sub> line of the Torsion Design block still comes from the St Venant-only diagram of `a.tors` (identical for statically determinate and symmetric layouts; the FE's own total end torques are printed on the End torques row); combined torsion with N<sub>Ed</sub> or an imposed M<sub>z</sub> remains blocked; the BS 5950 path has no torsion check.

## 19 Sep 2026 verification campaign

Full campaign over the gap-closure checks of groups G1–G4 on branch `ms-brief-standard-mcr`, on top of the G4 commit `78ee2a8`. Deliverables: `tests/batch/cases.cjs` (122 → 170 cases: WEB-01..08, PAT-01..07, UPL-01..05, TFB-01..04, HSV-01..07, TOR-01..06, AEF-01..05, BIX-01..05, UB-52 — every new check exercised in a passing and a failing / blocked direction), `tests/batch/run-batch.cjs` (cross-checks viii-FRd extended to a load over a support, PFC and RHS; xii-MVN to Class 3 I/H and RHS; new xiv-pattern (Clapeyron support moments and reactions of every pattern combination) and xv-MvRd (high-shear M<sub>v,Rd</sub> of every family); xiii-torsionFE with the cantilever uniform-torque closed form), `tests/batch/hand-checks.cjs` + `hand-checks.md` (16 cases, 29 quantities recomputed from the section table rows with every step written out), `tests/batch/mcr-method-comparison.md` (every eigen / standard M<sub>cr</sub> outlier explained, direction of conservatism stated), `tests/campaign.test.cjs` (8 tests pinning the hand-check figures, the verdict directions and the two findings below), `tests/batch/README.md` and `results.md` / `results.json` (170 cases / 296 runs: PASS 205, FAIL 71, NOT VERIFIED 20, ERROR 0; 0 cross-check mismatches, 0 trigger mismatches).

**Hand checks:** 29 comparisons, every difference below 0.001 % (web bearing F<sub>Rd</sub> types (a), (b), (c) and the RHS two-web lever rule; two- and three-span pattern moments and reactions by the three-moment equation; SN003a M<sub>cr</sub> with the C<sub>2</sub>z<sub>g</sub> term for the SS-UDL, fixed-ended UDL and fixed-ended point-load rows; SN006a cantilever with the Eq (7) q + F interaction; PFC N<sub>cr,T</sub>, N<sub>cr,TF</sub>, N<sub>b,T,Rd</sub>; RHS high-shear M<sub>v,y,Rd</sub>; A<sub>eff</sub>; the Vlasov cantilever twist under a tip torque and under a uniform torque, the warping-fixed mid-span twist). No case or engine value was adjusted on account of a hand check.

**Findings and corrections (from the M<sub>cr</sub> method comparison, `tests/batch/mcr-method-comparison.md`):**

| # | Finding | Correction |
|---|---|---|
| F1 | Standard (closed-form) route: the Serna quarter-point / mid-span moment sample landed exactly on the jump of an applied in-span couple and returned the far-side ordinate (UB-27: 118.5 instead of 226.5 kN·m at L/2), so C<sub>1</sub> = 1.676 and M<sub>cr</sub> = 382.3 kN·m against the eigenvalue 317.5 — **unconservative by 20 %** (LTB 0.74 printed for a beam the FE puts at 0.87) | `mAtStation()` in `js/checks/eurocode-checks.js`: interior stations are read 1 µm either side and the larger ordinate kept (the envelope convention of the C<sub>1</sub> expressions); used by `sn003aC1` (quarter points, Serna) and `c1Inputs` (M<sub>o</sub>), by the brief's portion helper and by the batch's independent check (iii). UB-27: C<sub>1</sub> = 1.206, M<sub>cr</sub> = 275.0 (ratio 1.155, conservative), LTB 0.98 PASS; MIX-01 (couple at 3L/4) 273.3 → 257.3. Smooth diagrams change by < 10⁻⁶ (the three-span fixture of `tests/gap-closure.test.cjs` re-baselined at that level). The Table B.3 M<sub>s</sub> of `cmTableB3` (`a.Mh`) still samples one side; it matters only for N<sub>Ed</sub> ≠ 0 with a couple exactly at mid-span |
| F2 | Eigen route: the destabilising ×1.2 switch with every z<sub>g</sub> = 0 was silently dropped — UB-40 PASSed at 0.84 with the load at the shear centre while the same beam with the top-flange height entered (UB-52, z<sub>g</sub> = +266) fails at 1.06; the closed-form route applies L<sub>E</sub> × 1.2 (FAIL 1.24) | `js/08-mcr-eigen-patch.js`: a blocking message when `S.destab` is ticked and no load height is entered (contradictory input; the remedy is printed). UB-40 eigen is NOT VERIFIED; UB-52 exercises the entered height on both routes |
| F3 (open) | The SN003a form with fork ends at both ends of L<sub>E</sub> is evaluated for overhang layouts (a free end at x = L): UB-23, TOR-06, PAT-04, UPL-01/02 — conservative by 26–153 % in the library (ratios 1.36–2.53), but the assumption is not the code's | Recommendation: block overhangs on the closed-form route as unpublished-C<sub>2</sub> cases are blocked, or evaluate the back span as the segment between its supports; not changed here (verdict-affecting design decision, the eigen route covers the layout) |
| F4 (information) | UB-44 (point load at 0.35 L on the top flange): the closed form prints M<sub>cr</sub> 47 % above the eigenvalue because C<sub>2</sub> is unpublished; PASS is already refused (iii-zgBlock 101/101) | None |

**Verdict counts** (`tests/batch/results.md`): per family UB 105 / 43 / 12, UC 26 / 2 / 2, PFC 32 / 16 / 1, SHS 15 / 2 / 2, RHS 27 / 8 / 3 (PASS / FAIL / NOT VERIFIED); per method eigen 101 / 17 / 8, standard 76 / 41 / 9, restrained 28 / 13 / 3. Of the 222 pre-campaign runs 219 print identical rows; UB-27 and MIX-01 (standard) moved with F1, UB-40 (eigen) with F2.

**Still open after the campaign:** F3 (overhangs on the closed-form route); a digit-for-digit check of the clause-6 web chain against a published SCI P363/P364 or MasterSeries example (the hand checks are first-principles arithmetic, not a published benchmark); the Table B.3 mid-span sample at a jump; the SLS-only uplift interpretation, γ<sub>G,inf</sub>, cantilever L/180 and s<sub>s</sub> = B defaults of G1/G2 unchanged; the legacy `checksEC3()` and the non-SCI EC3 branch of `render()` remain unreachable and untouched.

## 19 Sep 2026 gap closure, review fixes (findings F1–F7 of the branch review)

Review of branch `ms-brief-standard-mcr` at the campaign commit `fa631fa` (seven findings: one high, two medium, four low). Every finding was reproduced in the harness before the change; regression tests with hand-computed expected values are in `tests/review-fixes.test.cjs` (5 tests) and `tests/torsion-fe.test.cjs` (2 new tests); the campaign library and cross-checks were adjusted where the fixes change the inputs they assume.

| # | Finding | Was | Now |
|---|---|---|---|
| F5 (high) | Warping-torsion FE: bimoment averaged across an interior warping-fixed node (`torsionFeRecover`) | At a node whose φ′ DOF is fixed the restraint applies a reaction bimoment, so B jumps there; the recovery still took 0.5(B<sub>left</sub> + B<sub>right</sub>), so the peak B<sub>Ed</sub> at an interior warping-fixed support (overhang / multi-span "warping restrained for torsion", or a single fixed support inside the member) was under-reported (6.8 % low on the default mesh in the review layout), printed at the adjacent node, and the mesh-convergence measure crept toward the true value instead of converging (4.4 % > 0.5 %), so PASS was blocked for every such layout | `torsionFeSolveOnce` returns `warpNodes` (nodes with φ′ fixed); `torsionFeRecover` treats an interior one like a torque jump for B: the one-sided element values at x − 0.01 and x + 0.01 mm, the larger-magnitude side at x. **Hand check** (tests/torsion-fe.test.cjs): 457×191×82, 8 m, single support at x = 4 m (root, warping fixed), tip torques +T at x = 0 and −T at x = 8 m: each half is the validation-2 cantilever, B(root−) = +T·a·tanh(l/a) = 1.80896×10¹⁰ N·mm², B(root+) = −1.80896×10¹⁰ (the average is exactly 0) — engine one-sided values within 1×10⁻⁴, max\|B\| at x = 4000, reaction bimoment = the jump 3.618×10¹⁰, mesh error ≤ 1×10⁻³; overhang layout (supports 0 fork / 6 m warping-fixed, 20 kN at the 10 m tip with e = 100 mm): the back span stays untwisted (B = 0), B(6 m+) = 3.0×10⁶ × 1858.57 × tanh(2.1522) = 5.427×10⁹ N·mm² (engine 5.4269 kN·m² at x = 6.000 m), tip twist 0.11727 rad, mesh error 2.8×10⁻⁵, PASS not blocked. The review layout (UDL torque + tip torque) now reports max\|B\| = 5.2563 kN·m² at 6.000 m on 240, 480 and 1920 elements (mesh error 2.8×10⁻⁵ / 1.1×10⁻⁵ / 8.8×10⁻⁶) |
| F1 (medium) | PFC torsional buckling length L<sub>T</sub> defaulted to L<sub>cr,z</sub> (`torsionalFlexuralBuckling`) | `lcrZFromRestraints()` derives L<sub>cr,z</sub> from restraints that hold v only (r.v !== false); a lateral-only restraint (φ unticked) shortened L<sub>T</sub> and inflated N<sub>cr,T</sub> / N<sub>cr,TF</sub> — a lateral-vs-torsional category confusion (EN 1993-1-1 6.3.1.4(1), EN 1993-1-3 6.2.3(5): l<sub>T</sub> is set by the twist / warping restraint at the ends of the torsional segment) | New `lcrTFromTwistRestraints(a)`: largest spacing of the points that prevent twist — every support plus every intermediate restraint with φ !== false, overhangs beyond the outermost counted double, null without intermediate twist restraints; `annexB2` hands L<sub>cr,T</sub> = min(that, L<sub>cr,y</sub>) to `torsionalFlexuralBuckling(…, LcrT)`, which uses it as the default (L<sub>cr,y</sub> when none), the user `S.LT` still overriding. Source printed as "spacing of twist restraints (supports and restraints with φ held)" / "L<sub>cr,y</sub> (no intermediate twist restraint)" / "user L<sub>T</sub>" (brief N<sub>cr.T</sub> row, report). **Hand check** (PFC 150×75×18, 6 m, N = 250 kN, restraints at 1.5 m centres; i<sub>0</sub>² = 61.5² + 24.0² + 52.8² = 7146.09 mm², N<sub>cr,y</sub> = 495.70 kN, β = 0.60988): v-only restraints → L<sub>T</sub> = 6 m, N<sub>cr,T</sub> = (81000×6.31×10⁴ + π²×210000×4.67×10⁹/6000²)/7146.09 = 752.85 kN, N<sub>cr,TF</sub> = 363.41 kN, λ̄<sub>T</sub> = 1.3135, χ<sub>T</sub> (curve c) = 0.3832, N<sub>b,T,Rd</sub> = 240.24 kN, N<sub>Ed</sub>/N<sub>b,T,Rd</sub> = 1.041 FAIL (before the fix 0.682 with L<sub>T</sub> = 1.5 m); v + φ restraints → L<sub>T</sub> = 1.5 m, N<sub>cr,T</sub> = 1317.21, N<sub>cr,TF</sub> = 419.31, N<sub>b,T,Rd</sub> = 265.23 kN, 0.943; twist restraints at 1.5 and 4.5 m only → L<sub>T</sub> = 3 m while L<sub>cr,z</sub> = 1.5 m. The gap-closure G3 fixture (180×75×20, no intermediate restraint) keeps L<sub>T</sub> = 4 m = L<sub>cr,y</sub>. Batch cross-check (x-NbT) derives L<sub>T</sub> from the case's twist restraints independently; the input placeholder and validation text say "spacing of the twist restraints" |
| F2 (medium) | Hold-down design force and the uplift PASS / BLOCK decision taken from the entered ULS combinations only (γ<sub>G</sub> = 1.35 on the relieving span) | EN 1990 6.4.3.1(4) / Table A1.2(B): a favourable permanent action takes γ<sub>G,inf</sub> = 1.0 (STR set B), 0.9 in the EQU set A (Table A1.2(A)); with 1.35G on the back span the printed "design the hold-down for R = …" was unconservative and a support lifting at 1.0G could be printed as PASS (the pattern note said the reduced-G case "must be added by hand") | `gammaInfCompanions(ulsCombos)` (js/04-checks.js): for every analysed ULS combination (patterns included) with a G factor above 1.0, two companions with factors.G = 1.0 ("[γ<sub>G,inf</sub> = 1.0, STR set B]") and 0.9 ("[γ<sub>G,inf</sub> = 0.9, EQU set A]"), the variable factors as entered, one γ<sub>G</sub> over the whole member (single-source rule). `analyse()` solves them (`a.ulsCompanions`, bending solver only) and feeds their reactions into the uplift record (`a.uplift`, `holdDownCheck`) and — the STR set-B ones — into `webTransverseCheck` (a relieving G raises the reaction where G lifts the support; a lifting support bears nothing). The moment / shear envelopes are NOT extended (printed limitation, `patterns.limitation`: add a reduced-G combination by hand where a relieving permanent action could increase a span moment). Brief: companions listed under the loads after the patterns, the hold-down row names the governing companion, "Uplift … no support lifts in any of the n combinations (incl. the k γ<sub>G,inf</sub> companions)"; report: the same; the SLS-only advisory now states that the companions do not lift the support either (the former "EQU set A not generated" text is gone). **Hand check** (457×191×82, supports 0 and 4 m, L = 6 m, G 3 kN/m + self-weight 0.8044, 30 kN Q at the tip; R<sub>1,G</sub> = 1.5w = 5.7066 kN, R<sub>1,Q</sub> = −15 kN): 1.35G + 1.5Q = −14.796 kN (the former printed force), 1.0G + 1.5Q = −16.793, 0.9G + 1.5Q = −17.364 kN = the printed hold-down force; G = 12 kN/m: +3.429 at 1.35G (no uplift), −3.293 at 1.0G, −5.214 at 0.9G → "Hold-down required: R = −5.21 kN" blocking without the box (previously the SLS-only advisory). Batch: PAT-04 (5 m back span + 2 m overhang) lifts support 1 by 0.08 kN in the EQU companion of "Q on the overhang only" and now declares its hold-down |
| F4 (low) | Pattern set without the influence-line case for the maximum reaction at an interior support of a 4+ span member | Only single, adjacent-pair and odd / even sets; for 4 spans the R<sub>2</sub> influence line is positive in spans 1, 2 and 4, so F<sub>Ed</sub> at that support (web bearing) was under-enveloped by the span-4 ordinate (+2–3 %) | `expandPatternCombos`: for n ≥ 4 segments and each interior support k the set {k−1, k} plus the alternate segments outward (k+2, k+4, …; k−3, k−5, …), labelled "Q on spans 1+2+4 only (max reaction at x = 5 m)", kind `reaction`; a set equal to a pair (middle support of 4 spans) is dropped by `patternKey`. **Hand check** (four 5 m spans, w = 10 kN/m, three-moment equation, u = wL²): spans 1+2+4: 4M<sub>2</sub> + M<sub>3</sub> = −u/2, M<sub>2</sub> + 4M<sub>3</sub> + M<sub>4</sub> = −u/4, M<sub>3</sub> + 4M<sub>4</sub> = −u/4 → M<sub>3</sub> = −u/56, M<sub>2</sub> = −27u/224, M<sub>4</sub> = −13u/224, R<sub>2</sub> = wL(1 + 50/224) = 1.22321wL = 61.161 kN; pair 1+2: M<sub>3</sub> = −u/28, M<sub>2</sub> = −13u/112, R<sub>2</sub> = wL(1 + 22/112) = 1.19643wL = 59.821 kN (+2.24 %); engine 61.1607 / 59.8214; 12 ULS combinations for the 4-span member, the web-bearing F<sub>Ed</sub> at x = 5 m taken from the reaction set. Three spans generate no reaction set (the pair covers it). The batch cross-check (xiv-pattern) recomputes every generated combination by Clapeyron, the new sets included |
| F3 (low) | Default stiff bearing length at a support = the flange width B | s<sub>s</sub> (EN 1993-1-5 6.3(1)) is set by the bearing, not by the beam; a default that can only be unconservative relative to a shorter real seating gave a PASS that depended on an unentered input (533×210×92: F<sub>Rd</sub> 612 kN at B = 209.3 against 301 kN at s<sub>s</sub> = 40 and 415 kN at 100) | A blank support entry is the **lower bound s<sub>s</sub> = 0** (as for point loads). F<sub>Rd</sub> rises monotonically with s<sub>s</sub> (l<sub>y</sub>, and k<sub>F</sub>(c), grow with it), so a station that passes at 0 is verified for any seating and keeps its verdict entry; a station that fails at 0 with s<sub>s</sub> not entered is **NOT VERIFIED** (blocking `c.unsupported` message with the s<sub>s</sub> = 0 values, `station.nv`), not FAIL — the seating is unknown, so "bearing stiffener required" would be wrong advice. Such stations are excluded from the two verdict entries (nothing to certify), printed in the station table with the tag NOT VERIFIED, and the block's derivation shows the worst evaluated station (`web.show`). Entered values are unchanged. **Hand check** (demo 457×191×82, R = 229.5 kN, s<sub>s</sub> = 0): type (c) k<sub>F</sub> = 2, F<sub>cr</sub> = 0.9×2×210000×9.9³/428 = 856.9 kN, l<sub>e</sub> = min(174.87, 0) = 0, first pass l<sub>y</sub> = 16√(9.6616 + 14.3113) = 78.34 → λ̄<sub>F</sub> = 0.4989 ≤ 0.5 → m<sub>2</sub> = 0, l<sub>y</sub> = 16√9.6616 = 49.73, λ̄<sub>F</sub> = 0.3975, χ<sub>F</sub> = 1, F<sub>Rd</sub> = 275×49.73×9.9 = 135.4 kN, F<sub>Ed</sub>/F<sub>Rd</sub> = 1.695 → NOT VERIFIED; 203×133×25, 4 m, 5 + 5 kN/m passes at s<sub>s</sub> = 0 and is PASS. The DEMO state now carries s<sub>s</sub> = 100 mm at both supports as an entered value (F<sub>Rd</sub> = 437.3 kN, 0.525; the audit's demo utilisations unchanged); the batch library declares s<sub>s</sub> = 100 mm at every support unless a case sets its own (`LIB_SS` in cases.cjs; WEB-02 and HSV-01 state 150 mm seatings, which their reactions need); the support hint and the `[verify]` texts are replaced |
| F7 (low) | Support-list placeholder "(blank = B = …)" stale after a section change | `refreshAutoFields()` re-rendered the load list only | The label no longer depends on the section ("blank = lower bound 0: NOT VERIFIED if it fails"), so nothing can go stale; `renderSupportList()` no longer reads the section (test: the label is identical for 457×191×82 and 610×229×101) |
| F6 (low) | Automatic patterns multiplied the per-combination eigen and warping-torsion FE solves; `render()` synchronous on every input event | Not a correctness defect (batch within budget); an interactive recompute of a multi-span eccentric eigen case re-solved everything on an unrelated input | (1) Memo caches: `mcrEigenMemo` (js/08-mcr-eigen-patch.js, 96 entries) keyed by every input of the solve including the sampled moment diagram; `warpingTorsionFEMemo` (js/04-checks.js, 64 entries) keyed by the FE input; identical patterns of another parent, the shape-only solve of a z<sub>g</sub> = 0 combination (same key as the actual solve) and a re-render after an unrelated input (deflection divisor, grade, s<sub>s</sub>) are served from the cache (two-span overhang eccentric eigen probe: 4 eigen + 6 FE solves on the first render, 0 + 0 with 7 + 6 cache hits on the second; the batch campaign 339 s → 50 s). (2) Solve counts printed: brief "M<sub>cr</sub> = FE eigenvalue" row "n combination(s), k solve(s) + m cached", "Torsion analysis" row "k FE solve(s) + m cached"; report LTB heading. (3) `recompute()` debounced (250 ms `setTimeout` instead of `requestAnimationFrame`; print cancels it). Not done: skipping pattern combinations on the eigen route by a moment fraction (a design decision on the envelope, left open) |

**Verdict impact** (tests/batch/README.md "Results of the current run"): 29 runs change their governing utilisation, all through the web-bearing seating (100 mm declared instead of the flange width): UB-14 (eigen) and UC-07 (both routes) now FAIL the 7.2 interaction at the interior support (0.994 → 1.115, 0.990 → 1.210), PAT-04 declares its hold-down (F2); PFC cases with v + φ restraints are unchanged by F1 (their L<sub>T</sub> equals the restraint spacing); the F5 layouts (UB-51, TOR-04, PFC-21, TOR-06) are unchanged because their warping-fixed nodes are member ends or fork supports.

**Still open after the review fixes:** the moment / shear envelopes do not include the γ<sub>G,inf</sub> companions (reactions only, printed); the pattern set is still Q-only (no W / E patterns); the eigen route solves every pattern combination (no moment-fraction screening); the s<sub>s</sub> lower bound treats a blank entry as 0 for end and interior supports alike (an interior seating is usually longer, but no value is invented); F3 of the campaign (overhangs on the closed-form route) remains open.

## 19 Sep 2026 scope change: one span, six degrees of freedom per end (engine half)

Owner's decision of 19 Sep 2026: the tool handles ONE span, from End 1 (x = 0) to End 2 (x = L). Each end carries six degrees of freedom, each independently RESTRAINED or FREE — U<sub>x</sub> (axial translation), U<sub>y</sub> (lateral translation), U<sub>z</sub> (vertical translation), R<sub>x</sub> (twist), R<sub>y</sub> (rotation about the major axis, in-plane), R<sub>z</sub> (rotation about the minor axis, lateral bending) — plus warping (&phi;&prime; = 0). Internal hinges stay (in-plane moment release, lateral / twist continuity kept). Intermediate supports, overhangs, multi-span logic and the automatic span pattern loading are removed, not hidden. Branch `ms-brief-standard-mcr`, on top of `371ca97`. This is the engine half; `index.html` is untouched (the three controls it still carries for the old model — the "Root warping" select, the "+ Add support" button and the "Automatic pattern loading" switch — are hidden by `syncInputs()` with a `// STAGED: 19 Sep 2026` note until the UI half removes them; the four preset buttons drive the new presets through `data-preset`).

| Item | Was | Now |
|---|---|---|
| State model (`js/03-state-ui.js`) | `S.supports` list of `{pos, type: 'pinned'|'fixed', ss, stiff, holdDown, warpFix, vp, phip}`, `S.rootWarp`, `S.fixedLateral`, `S.autoPattern`, `S.leFactor = 1.0` | `S.ends = {e1: {ux, uy, uz, rx, ry, rz, warp, holdDown, ss, stiff}, e2: {…}}` (true = restrained). Presets as pure functions `endsPreset(name, {e1, e2})`: `ss` (E1 U<sub>x</sub>, U<sub>y</sub>, U<sub>z</sub>, R<sub>x</sub>; E2 U<sub>y</sub>, U<sub>z</sub>, R<sub>x</sub>), `fixed-fixed`, `fixed-pinned` (propped cantilever), `cantilever` (E1 all seven, E2 free), `guided-fixed` (alias `fixed-guided`: E1 fixed, E2 R<sub>y</sub> held with U<sub>z</sub> free, U<sub>y</sub>/R<sub>x</sub>/R<sub>z</sub> held), `pinned-guided`. Every preset restrains U<sub>x</sub> at End 1 only (statically determinate axial path) and warping only at a cantilever root. `DEMO` uses `ss` with s<sub>s</sub> = 100 mm at both ends. `S.leFactor` is blank (null) by default: the strut lengths come from the end fixities; a number overrides both axes (an entered 1.0 is an override — the former "leFactor === 1.0 means default" heuristic is gone). `S.rootWarp`, `S.fixedLateral`, `S.autoPattern` and the per-support v&prime;/&phi;&prime; boxes are deleted: their meaning is the end DOF flags |
| In-plane solver (`js/01-computation-engine.js`) | `solveBeam` knew `pinned` and `fixed` | `endsToSupports(S)` maps U<sub>z</sub> + R<sub>y</sub> &rarr; `fixed`, U<sub>z</sub> only &rarr; `pinned`, R<sub>y</sub> only &rarr; the new type `guided` (rotation held, vertical free: reaction moment, no vertical reaction), neither &rarr; free end (absent from the list). `solveBeam` honours `guided` (rotation DOF fixed only; V = 0 in the reaction row), `sfdBmd` closes the diagram through its reaction moment, an unknown type throws. The restraint-rank test is the pure `beamRestraintRank(L, supports, hinges)` (translation rows for pinned / fixed, rotation rows for fixed / guided), reused by the validation. Reactions carry `end` (1 or 2) |
| Validation (`endsStability()` in `js/04-checks.js`, called by `validateInputs`) | Support-count heuristics | In-plane: no U<sub>z</sub> anywhere &rarr; "Neither end restrains vertical translation … mechanism"; U<sub>z</sub> at one end without R<sub>y</sub> anywhere &rarr; "End 2 has no vertical restraint and End 1 does not restrain rotation: mechanism …"; hinges through the rank test ("each hinge needs one more restraint unit"); a cantilever rooted at End 2 &rarr; "mirror the member". Out-of-plane (LTB checked): at least one end with U<sub>y</sub> + R<sub>x</sub>; U<sub>y</sub> at one end only = lateral cantilever, allowed only with R<sub>z</sub> + R<sub>x</sub> at that end, its warping condition printed as an advisory; no R<sub>x</sub> anywhere &rarr; torsional mechanism. Torsion: any torque needs R<sub>x</sub> at one end at least (one end = torsion cantilever on the FE). Axial: N<sub>Ed</sub> &ne; 0 needs U<sub>x</sub> at one end; both ends allowed and printed "axial statically indeterminate: N taken as applied"; a strut axis whose fixities form a sway mechanism throws when N<sub>Ed</sub> &gt; 0. Every unknown combination throws before a solve; nothing returns NaN |
| Combinations | `expandPatternCombos` (Q on each span / pair / alternate spans / max-reaction sets), `patternInfo`, `spanSegments`, `comboLoadPieces` with masks | Removed. `comboLoadPieces(combo)` is the plain per-load list; `gammaInfCompanions` (G at 1.0 STR set B and 0.9 EQU set A for the end reactions) and `holdDownCheck` stay; `a.companionNote` replaces `a.patterns.note` |
| LTB eigen boundary conditions (`ltbRestraintsFor`, `js/08-mcr-eigen-patch.js`) | Every support a fork; v&prime; from `S.fixedLateral` / per-support boxes, &phi;&prime; from `S.rootWarp` / per-support boxes | `ltbEndRestraints(S)`: v from U<sub>y</sub>, v&prime; from R<sub>z</sub>, &phi; from R<sub>x</sub>, &phi;&prime; from warping; a free end contributes nothing at x = L. The report prints the end boundary conditions (fork / laterally clamped / free). The bay isolation between intermediate LATERAL restraints is kept (utilisation = max of the whole-member eigen result and the worst isolated bay, as before); the "span by span (support to support)" wording and the multi-span notes are gone |
| Torsion (`js/04-checks.js`, `js/checks/torsion-fe.js`) | &phi; = 0 at every support, &phi;&prime; = 0 by the per-support `warpFix` or automatically at a single fixed support; P385 closed forms for two end supports | Twist restraints = the ends with R<sub>x</sub> (`twistEnds(S)`), &phi;&prime; = 0 where that end's warping flag is set; `torsionFeSupports` no longer infers a root from the count. The P385 closed forms apply only with U<sub>y</sub> + R<sub>x</sub> at both ends and warping free at both; otherwise the FE (`feReasons`: "twist restrained at End 1 only (torsion cantilever)", "warping-fixed end", partial-span torque) |
| Strut effective lengths (`lcrDefaults(S)`, `js/03-state-ui.js`; `annexB2`, BS 5950 strut) | L<sub>cr</sub> = L<sub>E</sub>-factor &times; L, cantilever 2.0 L while the factor was 1.0 | From the end fixities, P360 Table 6.2 / BS 5950 Table 22 style, y-y from U<sub>z</sub>/R<sub>y</sub> and z-z from U<sub>y</sub>/R<sub>z</sub>: held in position at both ends — 0.7 L (direction held at both), 0.85 L (one), 1.0 L (neither); held in position at one end — 2.0 L when that end is held in direction and the other is free, 1.2 L when the other end is held in direction but not position (guided, sway), 2.0 L (theoretical, not a Table 6.2 row) for a pinned held end with a guided other end; position held at one end with direction at neither, or at neither end — mechanism (error with N<sub>Ed</sub> &gt; 0). Printed with the basis text and [verify]; the entered L<sub>E</sub>/L overrides both axes; the minor axis is still capped by the lateral-restraint spacing (P360 6.2). Cantilever N<sub>b</sub> uses 2.0 L from the fixities (replaces the leFactor === 1.0 heuristic). BS 5950 path: the same defaults for the strut (&times;1.2 destabilising kept there as before); LTB L<sub>E</sub> on the closed-form / BS routes = the entered factor (blank = 1.0) &times; L |
| Deflection | Per segment (span between supports, cantilever / overhang) | One span: span/`S.divisor` when both ends hold U<sub>z</sub>; L/`S.divisorCant` (default 180 [verify]) when an end is vertically free (cantilever tip or guided tip: value = tip deflection relative to the held end); the absolute cap unchanged |
| Every consumer of `S.supports` | web transverse-force stations, restraint forces, hold-down, deflection, `c1Segment`, `sn003aC1` end recognition, `chanMcrOK`, `cmTableB3` simple-span test, `lcrZFromRestraints` / `lcrTFromTwistRestraints`, diagram, 3D view, brief and report support rows, batch runner | Read the ends: web stations at the ends holding U<sub>z</sub> ("End n reaction", a guided end has no station); restraint forces at the ends holding R<sub>x</sub>; hold-down rows "at End n"; `sn003aC1` fixed-ended = R<sub>y</sub> at both ends; channel M<sub>cr</sub> route = fork ends holding U<sub>z</sub>; bay points = ends holding U<sub>y</sub> + intermediate v-restraints; brief loading list opens with "End restraints: End 1 (x = 0 m): U<sub>x</sub>, … restrained; End 2 …"; report prints the same line and the reactions per end ("guided: no vertical reaction"). `tests/harness.cjs`: `reset()` deep-copies its overrides and `ends(preset, overrides)` builds an ends object through the app's own `endsPreset()` |

**Behaviour changes to note (all deliberate):** (1) the cantilever preset restrains the root warping (seventh flag), so a cantilever's M<sub>cr</sub> rises against the former "root warping free" default — untick warping at End 1 for the old value (batch: UB-49, UC-04, PFC-09, TOR-01 (FAIL &rarr; PASS), TOR-05); (2) one warping flag per end drives both the LTB eigen &phi;&prime; = 0 and the torsion FE (the two separate boxes listed as an open item after G4 are merged: UB-51, TOR-04 higher M<sub>cr</sub>); (3) fixed-ended and propped struts take 0.7 L / 0.85 L by default instead of the entered 1.0 L (UC-12, MIX-07 (FAIL &rarr; PASS)); enter L<sub>E</sub>/L = 1 to keep the former figure; (4) the batch library lost its 23 multi-span / overhang / pattern cases and rewrote five (see tests/batch/README.md).

**Numerical evidence** (tests/structural.test.cjs, tests/gap-closure.test.cjs, tests/gap-closure-g3.test.cjs, tests/review-fixes.test.cjs, tests/torsion-fe.test.cjs, tests/web-transverse.test.cjs, tests/campaign.test.cjs; every figure [hand-derived]): fixed – guided 457&times;191&times;82, L = 6 m, w = 10 kN/m: M<sub>fixed</sub> = &minus;wL&sup2;/3 = &minus;120.0 kN&middot;m, M<sub>guided</sub> = +wL&sup2;/6 = +60.0 (sagging), M<sub>mid</sub> = 22.5, V(L) = 0, R<sub>guided</sub> = 0, tip deflection wL&#8308;/(24EI) = the mid-span value of the 2L fixed-ended beam (engine identical to 1e-6); pinned – guided: R<sub>1</sub> = wL, M<sub>guided</sub> = wL&sup2;/2; fixed – fixed wL&sup2;/12 and cantilever PL&sup3;/3EI unchanged; presets &rarr; support lists as tabulated; `lcrDefaults`: ss 1.0/1.0, fixed-fixed 0.7/0.7, fixed-pinned 0.85/0.85, cantilever 2.0/2.0, fixed-guided 1.2 (y-y) / 0.7 (z-z), pinned-guided 2.0 / 0.85, override 1.5 both, U<sub>y</sub> at one end without R<sub>z</sub> &rarr; z-z mechanism; laterally clamped ends (R<sub>z</sub> at both) on the 8 m 457&times;191&times;82: eigen M<sub>cr</sub> 258.0 &rarr; 451.8 kN&middot;m (UDL, ratio 1.751) against the SN003a k = 0.5 factor 1.725 (C<sub>1</sub> 1.127 &rarr; 0.972, k<sub>w</sub> = 1) — 1.5 %; central point load 310.5 &rarr; 492.7 (ratio 1.587 vs 1.558, 1.8 %); one clamped end in between, warping-fixed ends higher again; k<sub>c</sub> floor on a single span: UDL + hogging end couples &plusmn;160 kN&middot;m (mid-span M = 0) gives eigen C<sub>1</sub> = 4.70 and Serna C<sub>1</sub> = &radic;(35&times;240&sup2;/(240&sup2; + 9&times;60&sup2; + 9&times;60&sup2;)) = 4.70, both floored at 0.602; a propped cantilever with a hinge at 3 m of 8 m is floored too (a hinge at 2 m changes nothing: the elastic propped cantilever already has M = 0 at L/4); hold-down: a clockwise 90 kN&middot;m couple at End 2 of a 6 m simply supported span lifts End 1 by R = &minus;M/L = &minus;15 kN (&minus;22.5 at 1.5Q), blocking without the box, advisory with it, SLS-only with G = 10 kN/m holding it down at 1.35 / 1.0 / 0.9 G; &gamma;<sub>G,inf</sub> companions on the same layout with G 3 kN/m + self-weight: 1.35G &minus;7.09, 1.0G &minus;11.09, 0.9G &minus;12.23 kN (design force), G = 6 kN/m holds at 1.35G (+5.06) and lifts at 0.9G (&minus;4.13, blocked); mechanisms / lateral cantilever / torsion without R<sub>x</sub> / N<sub>Ed</sub> without U<sub>x</sub> throw the messages quoted above; torsion cantilever on the pure FE (twist + warping held at x = 0): tip twist (T/GI<sub>T</sub>)[L &minus; a tanh(L/a)] and root bimoment T a tanh(L/a) unchanged (0.39090 rad, 1.80896&times;10&sup1;&#8304; N&middot;mm&sup2;); WEB-07 rewritten: 300 kN over End 2 of a 3 m span, F<sub>Rd</sub> = 202.226 kN (type (c) governs over (b)), F<sub>Ed</sub> = R<sub>2</sub> = 460.926 kN. Demo figures of the original audit unchanged (0.3035 / 0.9122 / 0.6099, web 0.5248).

**Gates:** `node --test tests/*.test.cjs` 110 tests green (9 files); `node tests/batch/run-batch.cjs` 148 cases / 252 runs, PASS 181 / FAIL 51 / NOT VERIFIED 20 / ERROR 0, 0 cross-check mismatches, 0 trigger mismatches (28 s); `node tests/batch/hand-checks.cjs` 24 comparisons, none above 0.001 %; `pwsh ./build-single-html.ps1` reproduces `dist/beam-design-single.html` and the parity test passes.

**Still open after the scope change:** the UI half (closed by the section "19 Sep 2026 scope change, UI half" below); the strut-length table is an engineering interpretation of P360 Table 6.2 / BS 5950 Table 22 printed [verify] (the pinned + guided 2.0 L row is the theoretical value); the LTB validation allows U<sub>y</sub> + R<sub>x</sub> at one end with U<sub>y</sub> only at the other (a fork-free far end, an unusual support) — the eigen solver models it; the BS 5950 LTB effective length still comes from the entered factor (Table 13 end conditions are not derived from the flags); the guided-end deflection limit reuses the cantilever divisor (a sliding end of a symmetric 2L member); F3 of the campaign is closed by the scope (no overhangs).

## 19 Sep 2026 scope change, UI half: the End conditions panel, the brief / report lines

Completes the owner's single-span decision on the page side (engine half: the section above, commit `18d9401`). `index.html` no longer carries any control of the old support-list model.

| Item | Was (engine half) | Now |
|---|---|---|
| Geometry panel (`index.html`, `js/03-state-ui.js`, `js/07-wiring.js`, `css/beam-design.css`) | End rows rendered into the old `supportList` container by `renderEndsList()`; "+ Add support", "Automatic pattern loading" and "Root warping" hidden by `syncInputs()` with a STAGED note; four quick buttons through `data-preset` aliases (`cant`, `propped`, `fixed`) | Panel "Geometry &amp; end conditions": a preset drop-list `#endPreset` (Simply supported, Fixed-fixed, Fixed-pinned (propped cantilever), Cantilever, Guided-fixed, Pinned-guided, Custom), the four quick buttons on the canonical preset keys, and `#endsPanel` with two columns "End 1 (x = 0 m)" / "End 2 (x = L = … m)", each: a glyph, the seven boxes U<sub>x</sub> U<sub>y</sub> U<sub>z</sub> R<sub>x</sub> R<sub>y</sub> R<sub>z</sub> Warping (checked = restrained; long label as tooltip), and for an end holding U<sub>z</sub> the "hold-down provided" box plus (EC3) the stiff bearing length s<sub>s</sub> and the "bearing stiffener provided" box ("no vertical reaction (U<sub>z</sub> free)" otherwise). The three superseded controls, the STAGED block, `torsionWarpInputsOn()` (the warping box is always one of the seven) and the `PRESET_ALIAS` map are removed; the "Root warping" select is gone from the LTB panel (its injected "Fixed supports restrain lateral bending" box went with the engine half); the intermediate lateral restraint list stays. Hint texts rewritten for one span (loads, combinations, L<sub>E</sub>/L, deflection, LTB standard / eigen, hinges); the intro paragraph names the seven flags |
| Panel state (`js/03-state-ui.js`) | Flags only; the preset name derived by `endsPresetName()` | `S.endPreset` (`DEMO`: `'ss'`) names the drop-list selection. Pure mutators: `applyEndPreset(st, name)` (flags of both ends from the preset, the seating / hold-down / stiffener entries kept, the hinges cleared, `st.endPreset = name`, alias `fixed-guided` canonicalised, an unknown name throws before touching the state), `setEndDof(st, key, dof, on)` (one flag; `st.endPreset = 'custom'`; a bad end or DOF throws); `endPresetSelection(st)` = `'custom'` after a hand edit, else the preset the flags match, else `'custom'` &mdash; the flags are the truth, a stored preset whose flags no longer match (a harness fixture) is never shown. String builders without DOM: `endsPanelHtml(st)` (the two columns), `endGlyphSvg(e)` (in-plane symbol: fixed wall, pin with U<sub>x</sub> held or roller with U<sub>x</sub> free, guided slider, free end; "LTB: fork / clamped / + warping / partial / free"), `endsConditionsLine(st)`. `renderEndsPanel()` binds them (checkbox &rarr; `setEndDof` &rarr; rerender; hold-down / stiffener / s<sub>s</sub> &rarr; state only). `endsDescription()` removed (superseded by `endsConditionsLine()`) |
| Brief (`js/06-brief-masterseries.js`) | "End restraints" line inside the loading list; reactions as a note under the Member Forces table (`M = -r.M/1e6` at both ends) | Title block line 4 "End conditions: End 1: U<sub>x</sub> U<sub>y</sub> U<sub>z</sub> R<sub>x</sub> restrained (pinned in plane; LTB fork); End 2: … &mdash; simply supported[; internal hinge at x m …]" (W = warping; derived in-plane type fixed / pinned / guided / free and LTB type fork / laterally clamped / warping fixed / partial / free; preset name or "custom end conditions"). Member Forces table: rows labelled "End 1, x = 0" / "End 2, x = L" and two new columns "Reaction R (kN), M (kN.m)": R and M for a fixed end, R and &mdash; for a pinned end, "guided: M only" and M for a guided end, "free: none" for a free end, &mdash; when the portion end is not the member end (governing bay); the note states the conventions. The isolated-bay &chi;<sub>LT,mod</sub> tag reads "isolated bay: f = 1"; "support" wording in the web-bearing notes reads "end" |
| Report (`js/06-render.js`) | "End restraints" note under the loading sketch; reactions line with the same one-sign moment | "End conditions: …" note under the report title (`endsConditionsLine`); reactions line per end ("R = … kN, M = … kN m" / "(pinned: no M)" / "guided: M only, M = …" / "free end, no reaction") with the conventions in its heading |
| Reaction moment sign (`js/01-computation-engine.js`) | Both renderers printed `-r.M/1e6` at both ends: the nodal reaction is anticlockwise-positive on the beam, so a fixed-fixed UDL printed &minus;wL&sup2;/12 at End 1 and +wL&sup2;/12 at End 2 for the same hogging moment | Pure `reactionEndMomentKNm(r)` = `(r.end === 2 ? r.M : -r.M)/1e6`: the end bending moment in the diagram convention (sagging positive, hogging negative) at both ends |
| Docs, batch library | | README scope paragraph and file list; docs/BRIEF_MAPPING.md 5.0 / 5.1 / 5.6 / 7; docs/VERIFICATION_REPORT.md addendum and the eigen-route limitation bullet; editing-guide.html rows for js/03 and js/07. tests/batch/cases.cjs: UB-18 ("warping free at root") now unticks the root warping the cantilever preset restrains (it stayed deflection-governed, 0.830, both routes) and UB-19 loses its dead `rootWarp` key; batch counts unchanged |

**Numerical evidence** (tests/ui-state.test.cjs, tests/brief.test.cjs; [hand-derived]): 6 m span, 1.5 &times; 10 kN/m (no G, so no self-weight in the ULS case): fixed-fixed R = 45.00 kN and M = &minus;wL&sup2;/12 = &minus;45.00 kN&middot;m printed at BOTH ends (the old print gave +45.00 at End 2); fixed &ndash; guided M<sub>1</sub> = &minus;wL&sup2;/3 = &minus;180.00, M<sub>2</sub> = +wL&sup2;/6 = +90.00 (sagging) with "guided: M only", R<sub>1</sub> = wL = 90.00; pinned &ndash; guided R<sub>1</sub> = 90.00, M<sub>2</sub> = +wL&sup2;/2 = 270.00; cantilever R<sub>1</sub> = 90.00, M<sub>1</sub> = &minus;wL&sup2;/2 = &minus;270.00, End 2 "free: none". Presets &rarr; flags (ux uy uz rx ry rz warp): ss 1111000 / 0111000, fixed-fixed 1111110 / 0111110, fixed-pinned 1111110 / 0111000, cantilever 1111111 / 0000000, guided-fixed 1111110 / 0101110, pinned-guided 1111000 / 0101110; presets &rarr; solver lists as tabulated in the engine-half section. The six presets and a custom case (pinned + laterally clamped + warping-fixed End 1, guided End 2 with U<sub>x</sub>) render the brief in both M<sub>cr</sub> methods with no undefined / NaN, the block order intact and the End conditions line present. Browser smoke test on the modular page (Chrome, 1400 &times; 1000): no console errors; the drop-list, the quick buttons and the boxes drive the state (Cantilever &rarr; End 2 "no vertical reaction", ticking U<sub>z</sub> at End 2 &rarr; "Custom", propped-cantilever report); unticking R<sub>y</sub> at End 1 with End 2 vertically free prints "End 2 has no vertical restraint and End 1 does not restrain rotation: mechanism …"; Guided-fixed with LTB on prints "guided: M only | 612.06" against M<sub>1</sub> = &minus;1224.13 (wL&sup2;/6 against wL&sup2;/3 for the 8 m demo loads); BS 5950 drops the s<sub>s</sub> / stiffener fields and keeps hold-down; Reset restores the demo. Demo figures unchanged.

**Gates:** `node --test tests/*.test.cjs` 115 tests green (10 files; new tests/ui-state.test.cjs, one new test in tests/brief.test.cjs); `node tests/batch/run-batch.cjs` 148 cases / 252 runs, PASS 181 / FAIL 51 / NOT VERIFIED 20 / ERROR 0, 0 cross-check mismatches, 0 trigger mismatches (27 s, identical to the engine half); `node tests/batch/hand-checks.cjs` 24 comparisons, none above 0.001 %; `pwsh ./build-single-html.ps1` reproduces `dist/beam-design-single.html` and the parity test passes.

**Still open:** the drop-list stays on Custom after a hand edit even when the flags happen to match a preset again (the flags are printed, so nothing is hidden; choose the preset to rename it); the glyph draws the in-plane symbol and names the LTB type in words (no lateral / warping symbols); `S.memberName` still has no input (the brief's Member line falls back to the section string); the BS 5950 LTB effective length still comes from the entered factor; the guided-end deflection limit reuses the cantilever divisor; the strut-length table remains an interpretation printed [verify].
